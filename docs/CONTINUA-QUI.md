# CONTINUA QUI — stato lavori progetto Pebble

> Aggiornato: **30 agosto 2026** (Fase 1: watchface Galleria, sessioni S0–S7 completate; prossima S8 sull'orologio reale). Leggere insieme a `../CLAUDE.md`; il piano completo è `../PIANO-SVILUPPO-PEBBLE.md`.

## Stato: Fase 0 (setup) COMPLETATA ✅ — Fase 1 IN CORSO: watchface **Galleria** (`apps/galleria`)

> **Piano a sessioni: `apps/galleria/PIANO.md`** (leggere §8 "Stato" per la prossima sessione). Design: `docs/design/galleria.md`. Richiesta utente: `nuova-app-galleria.txt`.

### Ambiente installato in user space (nessun sudo)
| Componente | Dove | Versione |
|---|---|---|
| uv | `~/.local/bin/uv` (installato con `--no-modify-path`) | 0.12.5 |
| Python (gestito da uv) | `~/.local/share/uv/python/cpython-3.13.15-…` | 3.13.15 |
| pebble-tool | `~/.local/bin/pebble` → uv tool, pinnato (`tools/setup-env.sh`) | **5.0.40** (dal 30/08/2026; era 5.0.39) |
| SDK + toolchain ARM + QEMU | `~/.local/share/pebble-sdk/SDKs/4.33.1` (`current` → 4.33.1), 783 MB | **SDK 4.33.1** (firmware emulatore 4.33.2), xPack GCC 14.2.1, qemu-pebble 10.1.5-pebble14 |
| Librerie runtime QEMU | `~/.local/lib/pebble-deps/root/…` (libsdl2-classic 2.32.10, libxss1, libsndio7.0 estratte dai .deb) | — |
| Wrapper QEMU | `tools/qemu-pebble-wrapper` (usato via `PEBBLE_QEMU_PATH`, imposta `LD_LIBRARY_PATH` solo per QEMU) | — |
| Ambiente shell | `tools/pebble-env.sh`, caricato da un blocco aggiunto in fondo a `~/.bashrc` | — |
| Script idempotente | `tools/setup-env.sh` (rifà tutto quanto sopra; usarlo su una macchina nuova) | — |

Note: `~/.pebble-sdk` **non** esiste e non va creato (forzerebbe la dir legacy). Esiste un residuo `/var/tmp/pebble-sdk` di un test precedente in sandbox: non usato, ignorabile. `magick`/`convert`/`ffmpeg` **non** sono installati. Per quantizzare immagini alla palette a 64 colori usare **Pillow** (già presente nel Python di sistema, 12.1.1; oppure `uv run --with pillow`): ricetta in `tools/README.md` §6 (`Image.quantize(palette=…, dither=Image.Dither.NONE)`). `ffmpeg` serve solo alle GIF automatiche di `pebble publish` (alternativa: `--no-gif-all-platforms --screenshots …`).

### Verifiche fatte (24/08/2026)
- `pebble new-project --ai hello-emery` → `pebble build` ok (statico 833 B) → `pebble install --emulator emery` ok → screenshot 200×228 (`docs/fase0/hello-emery-emery.png`); idem **flint** (144×168) e **gabbro** (260×260): **tutti e 7 gli emulatori** sono presenti nell'SDK, `flint` e `gabbro` funzionano (la FAQ ufficiale è obsoleta).
- `apps/heapprobe` (sonda di memoria, `docs/ricerca/gap-1-memoria-emery.md` §6) eseguita su emulatori freschi — log completi in `docs/fase0/heapprobe-*.log`:

| | emery | flint |
|---|---|---|
| Build report "Total footprint in RAM" | 1.368 B | 1.364 B |
| Build report "Free RAM available (heap)" | 129.704 B | 64.172 B |
| `heap_bytes_free()` a `main` (used=24) | **129.680 B** | **64.144 B** |
| dopo `window_create` + layer (used=308) | 129.396 B | 63.860 B |
| `malloc` massima riuscita | **124 KiB** (free dopo 2.416) | **32 KiB** (64 KiB → NULL) |
| `malloc(128 KiB)` | NULL | NULL |
| blocchi da 8 KiB vivi insieme | 15 | 7 |
| `GBitmap` full-screen (8 bit / 1 bit) | OK, −45.640 B | OK, −3.400 B |

→ **Confermato il modello `heap ≈ 131.072 − statico − 24 B` (emery) e `65.536 − statico − 28 B` (flint)**; il framebuffer non pesa sull'heap; il build report è affidabile a meno di ~24–28 B. ❓ Resta da ripetere sull'orologio reale (atteso identico: stesso `Kconfig`).
- `pebble compile-commands --platform emery` genera `compile_commands.json` (per clangd/cpptools; ignorato da git).
- `pebble install --logs` su un emulatore **già avviato** non lancia l'app (resta evidenziata nel launcher): usare `pebble emu-button click select` o partire da `pebble kill`.
- (29/08/2026, S5b) **pypkjs**: `localStorage` persistente in `~/.local/share/pebble-sdk/4.33.1/<piatt>/localstorage/<uuid>.{dat,dir}` (`dbm.dumb`: chiuso pulito a ogni `pebble install`, **non** con `pebble kill`; in dev `index.js` fa `removeItem`+`setItem` — revisione 29/08: il padding a 64 KiB contava code unit UTF-16, `dbm.dumb` byte UTF-8); `pebble wipe` lo cancella insieme alla flash; `XMLHttpRequest` verso `localhost` funziona (`block_private_addresses` non è attivo); `Pebble.platform === 'pypkjs'`; `pebble emu-app-config` apre l'URL di `Pebble.openURL` con `?return_to=http://localhost:<porta>/close?` e consegna a `webviewclosed` la query grezza (≤ 64 KB): con `BROWSER=true` non apre finestre e si può rispondere con `curl` sulla porta (`ss -ltnp`); `emu-bt-connection --connected no` a metà sync lascia l'emulatore irraggiungibile (`pebble kill`).
- (26/08/2026, S1) Per una **watchface** invece `pebble install` su emulatore già avviato la **rilancia** (Select poi apre il launcher). L'emulatore emery conservava l'app di test `fonttest` di S0 che si appendeva ("fonttest is not responding") e impediva il lancio: **`pebble kill && pebble wipe`**. `pebble emu-bt-connection --connected no` **non** consegna l'evento a `connection_service` (né callback né `peek`): l'icona BT si prova con l'hook `GALLERIA_DEBUG_BT_OFF`. `pebble emu-steps N` e `pebble emu-set-time HH:MM:SS` funzionano (l'ora viene risincronizzata da pypkjs dopo pochi secondi). `pebble emu-set-content-size` ha effetto al riavvio dell'app (`preferred_content_size()` 1/2/3 loggato).

### Cose NON fatte / da fare a mano
- **Config page in un browser con display** (non headless) e su telefono vero: mai aperta (S6 solo Firefox headless via geckodriver); in S8 provarla sull'app Pebble Android/iOS.
- **Touch in emulatore**: verificare con click/drag del mouse nella finestra QEMU emery (nessun comando `emu-touch`); script QMP `tools/pbltouch.py` non ancora scritto (opzionale, vedi `docs/ricerca/gap-3-emulatore-touch.md`).
- **Default "Legacy Apps"** in Settings → Display dell'emulatore emery: non controllato.
- **Orologio reale**: nessun test (serve l'app Pebble sul telefono con Dev Connect; da Linux non c'è BLE).
- **CI** `.github/workflows/build.yml`: scritta, mai eseguita (nessun remote). **Git**: repo inizializzato (`main`), **nessun commit** — chiedere all'utente prima di committare; `git config user.name/email` globali non impostati.
- **SDK 4.17** installato il 30/08/2026 (S7, D5) accanto a 4.33.1: attivo resta **4.33.1** (`pebble sdk list`); per una build 4.17: `pebble sdk activate 4.17 && pebble clean && pebble build`, poi `pebble sdk activate 4.33.1`.
- ✅ Riferimenti in `tools/` (vedi `tools/README.md`, 8 sezioni): `pebble-watchface-agent-skill/` (skill ufficiale Claude Code: `SKILL.md`, 5 guide `reference/`, 10 template, 5 script, 7 progetti campione, tutorial C in 6 parti), `sdk-docs/` (215 MB: guide in `source/_guides/`, changelog in `source/_changelogs/`, Doxygen C API in `aplite|basalt/doxygen_sdk/`), `palette/pebble_colors_64.{gif,act,pal}` (verificata identica a `pebble_get_64color_palette()`), `svg2pdc.py` **Python 3 senza dipendenze** (parser SVG interno byte-identico a `svg.path==3.0`; ⚠️ `svg.path ≥ 4` rompe il tool originale; `--builtin-parser`), test in `tools/test/` (`python3 tools/svg2pdc.py test/icon.svg -o icon.pdc` → header `PDCI`, 3 comandi). I due cloni sono in `.gitignore`.

### Struttura attuale
```
Pebble/
├── PIANO-SVILUPPO-PEBBLE.md   CLAUDE.md   .gitignore   .github/workflows/build.yml
├── docs/  CONTINUA-QUI.md · ricerca/*.md (12 report) · fase0/ (log sonda, screenshot)
├── tools/ setup-env.sh · pebble-env.sh · qemu-pebble-wrapper · README.md · svg2pdc.py · palette/ · sdk-docs/ · pebble-watchface-agent-skill/
├── apps/  hello-emery/ (template --ai) · heapprobe/ (sonda memoria) · **galleria/** (watchface Fase 1: PIANO.md, CLAUDE.md,
│          src/c/{main,ui_time,ui_photo,ui_digits,settings,storage,model,sync}.c + puri {timefmt,photo_codec,crc,luma,rotation,sync_proto}.c + gal_types.h + gal_log.h (S7: LOGV/LOGH) + digit_metrics.h (generato),
│          src/pkjs/{index,album,sync,devserver,crc,b64}.js (S5b: album in localStorage + dev server; bundle webpack con entry index.js) + config_page.js (S6, GENERATO) + config/{page.html,page.css,pipeline.js,page_core.js,page.js,previews.js (generato)} (S6: config page, fuori dal bundle),
│          resources/photos/ (2 demo raw6/raw1 + README licenze), resources/fonts/ (3 TTF OFL + README), resources/digits/ (12 strip PNG compatte, S7), store/ (S7: icone 144/80/48 + screenshot store, make_assets.py), README.md (S7),
│          test/ (9 suite gcc fra cui test_sync di S7 + fixture Python↔C + shim/pebble.h esteso ad AppMessage/dict per storage/settings/sync_proto/model/sync su host; node: test_b64/test_album/test_sync_engine/smoke_s5b/test_page… con shim/{fakewatch,fake_pebble,message_keys}.js e fixture generate), wscript con hook GALLERIA_DEFINES)
├── docs/design/galleria.md (design v1.5) · docs/design/galleria/s1_*.png … s4_*.png (screenshot)
├── tools/photo_prep.py (v1: foto → raw6/raw1, FS serpentine come il JS di S6, anteprime sunlight, --stats/--fixture/--selftest; tools/README.md §9)
├── tools/gen_digits.py (TTF → strip sprite 2BitPalette + digit_metrics.h; interprete del venv pebble-tool; tools/README.md §10)
├── tools/galleria_devserver.py (S5b+S6: dev server = config page dell'emulatore, --page-dir/--relay, solo stdlib, --selftest; tools/README.md §11)
├── tools/galleria_browser.py (S6: Firefox headless via geckodriver, WebDriver stdlib; README §12) · tools/build_config_page.py (S6: inliner della pagina; README §13) · tools/gen_font_previews.py (S6; README §14)
├── docs/design/galleria-s6-config-page.md (specifica S6 v1.1 + precisazioni S7)
└── docs/design/galleria-s7-qa.md (specifica S7: obiettivi O1–O7, contratti, checklist del gate, esito)
```

## Prossimi passi (Fase 1 — watchface **Galleria**)
Seguire `apps/galleria/PIANO.md`, una sessione per volta (§8 "Stato" dice quale). Stato al 30/08/2026:
- **S7 (QA, memoria/perf, asset store) ✅ 30/08/2026**, compiti per importanza (alta → Fable: specifica `docs/design/galleria-s7-qa.md`, C sull'orologio — `gal_log.h`, D16 strip A a richiesta, hook `GALLERIA_DEBUG_TIMING`/`HEAP`, `storage_set_order` idempotente, statistiche per chunk nella riga END, glifi assenti —, triage e correzioni, gate e ri-gate, D5, documenti; media → Opus: `gen_digits.py --no-colon-b --pack`, `test_sync` + shim AppMessage/dict, `album.js` F12 + rollback + migrazione, config page #2 #11 #26 #31 #41, README, 5 lenti + 22 scettici; bassa → Opus: `fakewatch` clamp, asset store; 41 agenti in 4 workflow). **Risultati**: heap libero layout B emery **29.944 → 43.472 B** (A 47.072; flint A 28.640, B 28.208) con la strip A caricata solo sotto Quick View (D16, −7,9 KB), strip B senza `:` e tutte le strip **compatte** (`--pack`: risorse 145.732 → **125.200 B**), log asciugati (`LOGV`/`LOGH`, formati ≤ 80 caratteri, nessuna riga per chunk; statico 25.476 → **25.004 B**); `sync.c` compilato e testato su host (`test_sync`: 20 scenari, 1.400 asserzioni, 18/18 mutanti; timer di silenzio non allocabile → sync abbandonata subito); F12 (`fmts[fmt].stored`, nessuna rilettura dei payload nel piano), F9(4), clamp `dt`, `fakewatch` clamp, 5 migliorie della config page; matrice §12.1 + leak test (21 cicli QV, 20 shake, sync) verdi su emery e flint, timing emery ≤ 4 ms, **flint layout A 11–22 ms in emulatore** (riga info con font di sistema + alone: da misurare sul Pebble 2 Duo in S8); revisione 11 finding → 7 confermati e corretti (fra cui il **rollback a due formati di `album.js`**, regressione della prima stesura di F12), 4 refutati; ri-gate superato; README riscritto; `store/` con icone e screenshot (`pebble publish --screenshots` vuole nomi `<piatt>_…`); **D5**: build con SDK 4.17 verde e identica, gira su emulatore 4.17 e 4.33.2 → proposta di pubblicare con 4.17, **conferma dell'utente in S8**. Scoperte: `pebble sdk install` attiva da solo l'SDK (riattivare 4.33.1); `make -C test` con `SRC_DIR=<copia>` lascia binari stantii nel `build/` condiviso (usare `OUT=`); pypkjs stampa `Exception decoding QemuInboundPacket.footer` dopo `emu-tap`/QV (innocuo); `emu-set-time` sposta anche lo slot della rotazione stateless; in emulatore il tempo per foto della prima sync non è affidabile (i tempi per chunk sì). Dettagli in `PIANO.md` §4 (esito S7), §6 (S7), §7. **Prossima sessione: S8** (orologio reale, serve l'utente: iPhone per primo; D5 da confermare; licenza del codice da decidere).
- **S6 (config page) ✅ 30/08/2026**, compiti per importanza (alta → Fable: specifica con i contratti `docs/design/galleria-s6-config-page.md`, `src/pkjs/config/pipeline.js` byte-esatto, pagina `page.html/css/page_core.js/page.js`, `index.js`/`b64.js`/`album.js`, gate, triage e correzioni, documenti; media → Opus: `tools/build_config_page.py`, dev server `--page-dir`/relay/`POST /save` della pagina, `test/gen_page_fixture.py` + `test_pipeline.js`, `test_page.js`, revisione 5 lenti + scettici, `tools/galleria_browser.py`, correzioni ai tool, README; bassa → Opus: `tools/gen_font_previews.py`, foto di prova). **Cosa c'è**: la pagina di configurazione vera (ES5, 58,1 KB inlinati in `src/pkjs/config_page.js` da `tools/build_config_page.py` con strip dei commenti — `make -C test pagecheck` PRIMA di ogni `pebble build`), stato PKJS → pagina nell'hash dell'URL (`b64.encodeUtf8` di `album.state()` + piattaforma/formato/tetto), payload delta `{v:1, settings, order, deleted, photos}` con **un solo formato per foto** (quello dell'orologio collegato), trasporto `pebblejs://close#` sul telefono e `POST /save` + token in emulatore (dev server in **modalità relay**: `--page-dir src/pkjs/config`), pipeline immagine identica byte a byte a `photo_prep.py` (27 combinazioni, `test_pipeline.js`), gate nel browser con **Firefox snap headless via geckodriver** (`tools/galleria_browser.py`, WebDriver stdlib; foto sotto `$HOME` e fuori dai dot-dir: `~/galleria-gate/photos/`). Gate superato su emery, flint e percorso telefono (data: URL → `applyPayload` in node); revisione 43 finding → 18 corretti (fra cui Salva con stato non ricevuto che azzerava ordine e impostazioni), 25 refutati; ri-gate superato dopo le correzioni. Heap a regime invariato (A 45.240 B), statico invariato (nessuna modifica C), bundle PKJS 155,1 KB. Screenshot `docs/design/galleria/s6_*.png`. **Scoperte**: `Pebble.platform` è il runtime (`pebble`/`pypkjs`), non l'OS; il bundler webpack dell'SDK non include i file non richiesti; Firefox snap non legge `/tmp` né i dot-dir di `$HOME` e non scende sotto 500 px (`narrow`); `emu-app-config` scrive l'URL in `~/pebble-tool-emu-app-config-*.html` finché la pagina non torna su `/close`. Dettagli in `PIANO.md` §4 (esito S6), §6 (D11–D15), §7. **Prossima sessione: S7** (QA: matrice §12.1, heap layout B, log asciugati, `test_sync` su host, migliorie S6 elencate in §7, D5, README/icone).
- **Revisione post code-review ✅ 29/08/2026** (fra S5b e S6; tutti i compiti su Fable per richiesta esplicita dell'utente — deroga una tantum): `/code-review` → 15 finding, triage adversariale (31 agenti) → tutti confermati; **14 corretti** — C: rotazione congelata durante la sync (`model_sync_hold`, F1), timer idle scaduto-non-consumato (`cancel`+`register`+generazione, F2), `SYNC_REQUEST{OFFSET}` per "Foto k/n" (F3, v1.8 senza messageKey nuove), outbox esatta 110 B + tripwire (F4); PKJS: attesa HELLO con rinvii (F5), `cancelWait` in `finish` (F7), classi di retry link/photo/permanent con `photoCodes` e `photoRetries` (F9), payload prima della foto vecchia (F6), rollback se il JSON non si scrive (F8), `full` sicuro (F10), `removeItem`+`setItem` dev (F11), listener staccato in `_reset` (F14); dev server: pagina senza `settings` (F13), keep-alive 400/404 (F15); F12 → S7. Test: `test_model.c` nuovo (296) + `test_index_retry.js` 100, `test_devstorage.js` 74, `test_devpage.js` 318, `test_dumb_durability.py` 14; motore 643, album 957, smoke 55, sync_proto 2.493, storage 497, dev server 148 (`make -C test` ~45 s). Gate emery+flint verde (scenari, F1 con tick dentro la sync, `città.png` + kill, F4 negativo); statico **25.476 / 25.364 B**, heap A 45.240 B, B 29.944. Revisione adversariale dei diff (35 candidati: 10 confermati e corretti, 23 valutati a mano per limite di sessione); residui in `PIANO.md` §7 "Da riguardare a fine progetto". Nota operativa: `pkill -f galleria_devserver` da una Bash che contiene lo stesso testo uccide la shell (usare il PID). **Prossima sessione: S6** (prima: aprire davvero `/config.html` in Firefox).
- **S5b (sync lato telefono + dev server) ✅ 29/08/2026**, compiti per importanza (media → Opus: `tools/galleria_devserver.py` con scettico in 2 round + `--selftest` 86, `test/test_album.js` 541 e `test/test_sync_engine.js` 419 con mutation testing 48/49, revisione 5 lenti + scettici; alta → Fable: specifica design §5.1, C `HELLO{CRC}` v1.7, `crc.js`/`album.js`/`sync.js`/`devserver.js`/`index.js`, orologio finto `test/shim/fakewatch.js`, gate, verifica finding, documenti). Album del PKJS in `localStorage` (`galleria.v1.album` + `p<slot>.<fmt>` per formato), **diff stateless a ogni `HELLO`** (foto per CRC, impostazioni per `HELLO.CRC` + `settingsSet`, `ALBUM_ORDER` sempre completo con gli slot "estranei" accodati: il telefono non elimina mai ciò che l'utente non ha eliminato), motore con caricamento pigro/`resync`/`abortSync` e retry lungo in `index.js` (30 s → 10 min); dev server = config page dell'emulatore (`/state.json` full con `url`, `/photo/<k>?b64=1`, `POST /save` + token `{v:1,dev:true,seq}` nel ritorno di `emu-app-config`, `--scenario`). **4 bug reali** trovati dai test Opus e corretti (eliminazione+riempimento dello stesso slot, `orderDirty` al caricamento, `deleted` non filtrato, `photoFailed` su `s.cur` nullo) + **5 confermati dalla revisione** (52 agenti) e corretti: payload cancellato se l'album cambia durante la sync, nessun retry se il primo `JS_READY` non passa, `orderDirty` azzerato da un ordine superato (`orderGen`), invio fallito dei passi non-foto senza retry, STATUS di copie ritrasmesse attribuiti al passo successivo → **`STATUS.REPLY_TO`** (messageKey 16, v1.7). Gate emery: 2 foto vere dal dev server in 9 messaggi ciascuna (1,1–1,3 s, heap identico), riavvio senza rinvii, config page via dev server (delete/riordino/impostazioni in una sync), percorso telefono con payload da 46 KB nel ritorno della pagina, interruzione a metà (`--scenario interrupt`) → 30 s idle → watchdog 60 s → retry 30 s → foto rimandate; flint 2 foto raw1 in un messaggio l'una. Screenshot `docs/design/galleria/s5b_*.png`. **Scoperte**: `localStorage` di pypkjs **esiste ed è persistente** (`~/.local/share/pebble-sdk/4.33.1/<piatt>/localstorage/<uuid>`, `dbm.dumb`: l'indice si aggiorna solo alla chiusura pulita → padding dev-only a 64 KiB in `index.js`, altrimenti `pebble kill` tronca l'album); `pebble wipe` cancella anche il `localStorage`; `emu-bt-connection --connected no` a metà sync rende l'emulatore irraggiungibile dal tool; Firefox snap perde la query di `emu-app-config --file` (portale documenti) → ritorno simulato con `curl` (`BROWSER=true`). Statico 25.116 B emery (+104: `HELLO.CRC` + `STATUS.REPLY_TO`), heap A 45.580 B; bundle JS 76 KB (fixture spostata in `test/`). Test: C 3.288 + `sync_proto` 2.394, node b64 2.444 / album 560 / motore 437 / smoke 31, dev server 101. Dettagli in `PIANO.md` §4 (esito S5b), §6, §7. **Prossima sessione: S6** (config page vera; il PKJS accetta già il payload §6 e lo stato per la pagina è `album.state()`).
- **S5a (sync lato orologio) ✅ 29/08/2026**, compiti per importanza (media → Opus: shim esteso + `test/test_sync_proto.c` adversariale 1.456 asserzioni con mutation testing, `test/gen_sync_fixture.py` + `src/pkjs/b64.js` + `test/test_b64.js` 2.444, revisione 5 lenti + scettici; alta → Fable: protocollo §5 v1.6, `sync_proto.c` puro + `sync.c`, "Foto k/n", `main.c`, motore JS `src/pkjs/sync.js` + fixture `index.js`, gate, verifica finding). **Scoperta che cambia D9: `app_message_close()` non è esportata dall'SDK 4.33.1** (né `pebble.h` né `libpebble.a`) → nessuna inbox a due fasi: **una sola inbox** da 4.153 B (chunk 4.096 = 16 chunk persist/messaggio; flint 3.129) aperta in `init()`, chunk rinegoziato a ogni `JS_READY`; `app_message_open()` non ritaglia le dimensioni (PebbleOS). Il JS manda i `Number` come int32: tuple lette per `length`. Protocollo: `PHOTO_BEGIN` → sempre `STATUS{OK, OFFSET}`, `PHOTO_DATA` senza risposta (ACK), `SEQ_ERR` → `PHOTO_BEGIN{OFFSET}`, codice 7 `STORAGE_ERR`, 30 s di silenzio → IDLE. Gate emery (wipe → install): foto sintetica da 34.200 B in 9 messaggi, 1,03 s, 16 scritture persist per messaggio in 10–48 ms, commit 12 ms, mostrata subito e riletta da persist al riavvio (7 ms), HELLO col CRC → il PKJS non rimanda; scenari `seq`/`dup`/`crc`/`interrupt`/`order`/`delete`/`settings` tutti come da specifica (screenshot `docs/design/galleria/s5a_*.png`); flint: un messaggio, 159 ms. Revisione (5 lenti + scettici, 66 agenti): 8 finding confermati e corretti (deadlock del PKJS su `SYNC_READY` ripetuto + watchdog 60 s; slot VALID svuotato nel manifest al `PHOTO_BEGIN` di una sostituzione; ricarica del modello differita a fine sync; "Foto k/n" per identità; `PHOTO_END` idempotente con `PHOTO_ID`; `HELLO.FORMAT`; doc §5) — dettagli in `PIANO.md` §6. Bug trovati e corretti prima: 3 dai test host Opus (ritrasmissione di `PHOTO_BEGIN` che incrementava "Foto k/n", `SYNC_REQUEST` ripetuta senza notifica UI, guard inutile) + 1 dal gate (motore JS: dopo `SEQ_ERR` due messaggi in volo e STATUS tardivo scambiato per quello dell'END → "generazione" del flusso e filtro degli STATUS non pertinenti). ⚠️ **Heap**: statico 25.012 B emery (+6,6 KB), heap libero a regime **A 45.892 B, B 30.388 B** (sotto i 40 KB del vincolo: da recuperare in S7, `PIANO.md` §7); flint A 28.160 B. Dettagli in `PIANO.md` §4 (esito S5a), §6, §7. **Prossima sessione: S5b** (album vero nel PKJS + dev server; il motore `sync.js` e `b64.js` sono pronti, `index.js` fixture va sostituito da un provider sull'album in `localStorage`).
- **S4 (persist, rotazione, shake) ✅ 28/08/2026**, compiti per importanza (media → Opus: test adversariali `rotation`, shim persist `test/shim/pebble.h` + `test_storage`, 5 lenti + scettici; alta → Fable: semantica rotazione, `gal_types.h`, `rotation.c`, `storage.c`, `model.c`, `ui_photo_load_persist`, `settings_apply` + persist, `main.c`, gate, verifica finding): manifest unico 214 B (chiave 1, CRC16, scritto per ultimo), chunk `1000 + k·256 + i`, impostazioni con debounce 10 s + flush in deinit, `GalRotState` (shake) solo in deinit, quota ≥ 1 MiB altrimenti solo demo, schema futuro → reset; rotazione **stateless** su minuti locali (`days_from_civil`), sequenziale/casuale (Fisher–Yates per round, mai la stessa due volte per n ≥ 3), giornaliera alle 04:00, shake = `accel_tap_service` (offset in RAM), nessun cambio fuori focus; demo ruotano quando l'album è vuoto; hook `GALLERIA_DEBUG_SEED=1|2`, `GALLERIA_DEBUG_INTERVAL=1`, `GALLERIA_DEBUG_ORDER=1`, `GALLERIA_DEBUG_SETTINGS_SAVE=1`. Gate su emulatore emery+flint verde (seed → foto da persist `crc ok` in 8–13 ms, cambio al minuto, 10 shake con heap identico, impostazioni e shake ripristinati dopo il riavvio). Statico 18.363 / 18.251 B; heap libero 56.716 B (A) emery / 37.960 flint. Test host adversariali Opus (rotation 266, storage 474 con shim persist) hanno trovato 2 bug corretti: wrap del contatore shake (ora 16 bit modulo 27.720) e impostazioni perse se la scrittura persist falliva; la revisione a 5 lenti + scettici (45 agenti) ne ha confermato un terzo, corretto: schermo nero dopo un caricamento fallito (stato invalidato e slot ricaricato; hook `GALLERIA_DEBUG_CORRUPT_SLOT=1` per riprodurlo). **Scoperto: `persist_write_int/data` ritornano i byte scritti (non `S_SUCCESS`); `APP_LOG` troncato a ~100 caratteri.** Dettagli in `PIANO.md` §4 (esito S4) e §6. **Prossima sessione: S5a** (sync lato orologio).
- S0 (scaffold + ricerca + design) ✅: cartella compilabile (830 B statici), `PIANO.md` e `CLAUDE.md` dell'app, esperimento quantizzazione (dithering FS obbligatorio; `tools/photo_prep.py` v0), ricerca multi-agente salvata in `docs/ricerca/galleria/` (6 file), **`docs/design/galleria.md` v1.1** scritto (integra il brief di sintesi `docs/ricerca/galleria/07-brief-sintesi.md`), decisioni D1–D4 chiuse (config page in-app con file input; foto raw6/raw1 in `GBitmap` preallocato; cifre come sprite 2-bit da TTF; font Anton/Bebas Neue/Barlow Condensed + LECO sistema). Utente: Android **e** iPhone (S8 su entrambi); layout A confermato.
- S3 (layout full-screen + font multipli) ✅ 27/08/2026, compiti divisi per importanza (bassa/media → Opus: font, `tools/gen_digits.py`, verifica con build di prova, fix; alta → Fable: griglia 12 h, `ui_digits.c`, `ui_time.c` a 4 modalità, gate, revisione): 3 font OFL (Anton, Bebas Neue, Barlow Condensed Bold) → 12 strip `2BitPalette` (A 40×68 / B 64×96 emery, 28×44 / 48×64 flint; 11 celle, contorno 1 px) con metriche generate; `ui_digits.c` (sub-bitmap per glifo, palette riconosciuta dal colore, un blit `GCompOpSet`); layout A con sprite o LECO, layout B HH sopra MM (celle 64/48, "PM" in basso a destra), sotto Quick View riga singola con la strip A (`ui_time_unobstructed_changed` → `ui_time_band_changed`); hook `GALLERIA_DEBUG_LAYOUT/FONT`. Decisione 12 h: celle cifre 38 + `:` 16. Barlow A ridotto a 61 px (`4` largo 42). Statico 12.875 / 12.755 B; risorse 145.732 / 45.760 B; heap libero 62.204 B (A) / 46.908 B (B) emery, 43.872 / 36.608 flint. Screenshot `docs/design/galleria/s3_*.png`. Nota: `pebble emu-time-format --format 12h|24h` (non `12`/`24`). Revisione: 5 lenti Opus + verifica Fable (i subagenti Fable hanno esaurito il limite) → 15 finding tutti confermati e corretti (fra cui: riga info calcolata anche in B, fascia B ridotta a MM+PM, font effettivo unico, controlli metriche↔griglia, advance per glifo, `analyze-size` rotto con `sh` 2.4.0), dettagli in `PIANO.md` §6. **Prossima sessione: S4** (persist, rotazione, shake).
- S2 (sfondo foto + colore testo automatico + alone) ✅ 27/08/2026: `tools/photo_prep.py` v1 (pipeline identica al JS di riferimento per il round trip di S6), 2 demo raw6/raw1 come risorse `raw` per piattaforma (`Big_Dipper` scura → testo bianco, `Snowy_Ubuntu_Light` chiara → testo nero; **CC-BY-SA-4.0: solo test, da sostituire prima di S9**), moduli puri `photo_codec.c` (streaming con carry, `PhotoDecoder` con stride), `crc.c`, `luma.c` con test host (263/614/99 asserzioni + selftest Python 49; fixture cross-language), `ui_photo.c` (un solo GBitmap allocato per primo in `init()`: −45.664 B emery / −3.088 B flint; risorsa letta con UNA chiamata nella coda del bitmap e decodificata in place; sub-bitmap della fascia al tick), `ui_time.c` con colore automatico (luma sulla fascia intera), alone 8×/4× e colori fissi, hook `GALLERIA_DEFINES` nel wscript. Heap libero a regime 74.096 B emery / 51.256 B flint; statico 8.918 / 8.798 B; risorse 72.508 / 10.156 B. Repaint mirato con foto verificato (diff prima/dopo tick = solo le cifre). Bug trovato e corretto: `test/Makefile` di S1 non eseguiva i test (`run-%` in `.PHONY`). Screenshot in `docs/design/galleria/s2_*.png`. Revisione multi-agente (Fable) a fine sessione: 9 finding, 6 corretti (fascia sempre ripulita senza bitmap, lettura risorsa in una chiamata, reset luma a ogni foto, clip fascia, static_assert dimensioni, alone senza bitmap), dettagli in `PIANO.md` §6. **Prossima sessione: S3** (layout B + font sprite; prima decidere la griglia 12 h/AM-PM in `PIANO.md` §7).
- S1 (motore ora, layout A, riga info) ✅ 26/08/2026: `main.c`, `ui_time.c`, `settings.c` (`GalSettings` 20 B, `reserved[6]`), `timefmt.c` puro + `test/` (58 asserzioni verdi); LECO 60/42 di sistema come placeholder, riga info flessibile (passi/BT · batteria+fulmine · data con fallback), 12/24 h + zero iniziale, Quick View (layout invariante), content size, `did_focus` → redraw completo; **repaint parziale verificato** (marker persiste al tick). Statico 5.172 B, heap libero a regime 123.516 B emery / 57.980 B flint. Revisione multi-agente (Fable) a fine sessione: 7 finding confermati e corretti (icona BT fuori fascia su flint, `band_h`, AM/PM), 5 refutati (dettagli in `PIANO.md` §6). Screenshot in `docs/design/galleria/s1_*.png`. Poi S2 (vedi sopra).
- Poi S8 (orologio reale + batteria 48 h) → S9 (pubblicazione).
- Decisione SDK 4.17 vs 4.33.1 (D5): S7 ha verificato che la build 4.17 è identica e funziona; proposta 4.17, conferma in S8. SDK 4.17 è installato accanto a 4.33.1 (`pebble sdk activate <versione>`; `pebble sdk install` attiva da solo ciò che installa).
