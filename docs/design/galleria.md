# Design — watchface **Galleria** (`apps/galleria`)

> **v1.9 — 05/09/2026** (S8-perf: **schema persist 2** — manifest unico da 234 B con dentro le impostazioni, migrazione 1 → 2 una tantum, nessuna scrittura persist in `deinit`, **shake solo in RAM** (D10 rivista da D19), `HELLO.OPEN_MS` (17ª messageKey, outbox 119 B) e avviso «Galleria si avvia lentamente» nella config page con soglia proporzionale alle foto; S8-stile: D20–D26 — anello e ombra come 3° e 4° indice della palette delle cifre, font Francois One e Staatliches, `digit_style`, griglia uniforme adattata al font; v1.8 da S7: D16 strip A caricata solo sotto Quick View nel layout B, strip B a 10 celle, livelli di log `gal_log.h`, `storage_set_order` idempotente, `stored` nell'album del telefono, `test_sync` su host, config page con 5 migliorie, budget §8 rimisurato; v1.7 da S5b: §5.1 album del telefono e diff stateless, `HELLO.CRC` delle impostazioni + `settingsSet`, slot estranei mai eliminati, motore con caricamento pigro/`resync`/`abortSync` e retry lungo, dev server §6 come config page dell'emulatore, `localStorage` di pypkjs = `dbm.dumb`; v1.6 da S5a: protocollo §5 riscritto sul codice, D9 rivista — inbox unica da 4.153 B perché `app_message_close()` non è nell'SDK, codice STATUS 7, "Foto k/n"; v1.5 da S4: persist `storage.c` con API reali, rotazione stateless `rotation.h` su minuti locali, `gal_types.h`, `persist_write_*` ritornano i byte scritti, punto §10.4 chiuso; v1.4 da S3: sprite cifre da `gen_digits.py`, griglia 12 h, layout B + riga singola in Quick View, `digit_h` reale, budget aggiornato; v1.3 da S2: foto raw6/raw1 in `ui_photo`, luma sulla fascia intera, alone 8×/4×, `photo_prep.py` v1 come riferimento per il JS di S6, hook `GALLERIA_DEFINES`; v1.2 da S1; v1.1 da S0). Basato sulla richiesta `nuova-app-galleria.txt`, sulla ricerca multi-agente in `docs/ricerca/galleria/` (fonti primarie: sorgente `coredevices/mobileapp`, PebbleOS, pebble-tool/pypkjs, Chromium/WebKit, SDK 4.33.1; misure su emulatore e — per il persist — su Pebble Time 2 reale riportate dall'autore di Fields of Gold) e sul brief di sintesi indipendente `07-brief-sintesi.md` (che ha corretto tre punti della v1.0: origine opaca delle pagine `data:`, `emu-app-config` 5.0.39, limite URL iOS). Piano a sessioni: `apps/galleria/PIANO.md`. Regole generali: `PIANO-SVILUPPO-PEBBLE.md` §7–§10.
> Legenda: ✅ verificato su fonte primaria · ⚠️ inferenza/stima · ❓ da verificare sull'orologio/telefono reale.

## 1. Idea

Watchface (archetipo W + sync S) per **Pebble Time 2** (`emery`, 200×228, 64 colori) e **Pebble 2 Duo** (`flint`, 144×168 B/N). Foto scelte e ritagliate dall'utente **dal telefono** (config page nell'app Pebble), trasferite via AppMessage, **memorizzate sull'orologio** (persist, 12 slot) e mostrate **a schermo intero, a rotazione** con intervallo configurabile. Sopra la foto: **ora grande e nitida** in due layout — **A** "un terzo" (cifre 68 px + riga info passi/batteria/data) e **B** "tutto schermo" (HH sopra MM, cifre 96 px) — con **scelta font** (3 + LECO di sistema), **colore testo automatico** (bianco/nero in base alla foto, contorno quando serve), **12/24 h** e **zero iniziale**. `MINUTE_UNIT`, **mai i secondi**, nessuna animazione; tutto funziona senza telefono (foto e impostazioni in persist; 2 foto demo incluse). L'utente usa **Android e iPhone**.

## 2. Decisioni (con motivazione e fonte)

| # | Decisione | Perché | Fonte |
|---|---|---|---|
| D1 | **Acquisizione foto nella config page in-app**: pagina `data:text/html;charset=utf-8,` + `encodeURIComponent(html)` costruita **dal nostro PKJS** (l'app non aggiunge alcun prefisso), `<input type="file" accept="image/*">` **senza `capture`**, crop/tono/quantizzazione su `<canvas>`, ritorno `location.href = 'pebblejs://close#' + encodeURIComponent(JSON)` con le foto in **base64url**. **La pagina non può usare `localStorage`** (origine opaca → `SecurityError`): tutto lo stato (impostazioni, elenco slot, miniature) arriva dal PKJS nell'hash dell'URL e torna nel payload. | WebView in-app (Android System WebView / WKWebView), JS attivo, URL caricato così com'è; file input su **Android dall'app Pebble 1.8.0.7 (04/08/2026)** (PR #197; certo dal tag 1.9.1.3); su **iOS** lo gestisce WebKit (Libreria foto / Scegli file) ma **nessuno l'ha provato** ❓; la voce Fotocamera farebbe crashare l'app (manca `NSCameraUsageDescription`). Il PKJS su iOS è JavaScriptCore "nudo" (niente canvas). | `01`, `06` V1–V4 ✅ |
| D1b | **Limiti payload**: Android/Chromium scarta in silenzio URL > 2 MiB → budget ≤ 900 KB per Save; iOS: **nessun limite noto** (il limite WebKit di 2 MB del 28/07/2026 è stato revertito il 12/08/2026) → budget prudente ≤ 200 KB finché non misurato ❓. PKJS accetta `e.response` sia decodificato (app: `decodeURLPart`, che lancia su `%` malformato → mai `%` nel payload) sia percent-encoded (emulatore). | `06` V5–V6 ✅ |
| D1c | **Emulatore**: `pebble emu-app-config` del pebble-tool 5.0.39 **non apre pagine `data:`** e accetta al ritorno ≤ 65.514 B di query (oltre: 414 e resta appeso). → In emulatore il PKJS (`Pebble.platform === 'pypkjs'`) apre `http://localhost:8765/config.html` servita da **`tools/galleria_devserver.py`**; la pagina legge lo stato da `GET /state.json` (pushato dal PKJS con XHR POST), salva con `POST /save` e torna con un token di poche decine di byte; il PKJS scarica `GET /save.json` e prosegue **con lo stesso codice di sync del telefono**. | `06` V4, V7 ✅ (pypkjs non blocca gli indirizzi privati) |
| D2 | **Formato foto: raw 6 bit "packed" su emery** (4 px → 3 B MSB-first, riga 150 B, 34.200 B), **raw 1 bit palettizzato su flint** (MSB-first, 18 B/riga, 3.024 B). Stesso formato in trasferimento, persist e risorse demo. **Un solo `GBitmap` 8Bit preallocato in `init()`** (45.640 B) riempito a streaming (nessun buffer transiente, nessuna `malloc` a regime). PNG8 / 4BitPalette rimandati a v2 (campo `format` nel manifest). | Decoder PNG del firmware: solo il **primo IDAT**, **tinflate senza bound check**, bitmap non-NULL anche in errore, PNG intero in RAM. raw6: deterministico, −25 % vs 8 bpp, 64 colori pieni. 4-bit: 16 colori visibilmente peggiori. | `02` F1–F9, F24; `05` §0 ✅ |
| D3 | **Cifre come sprite `2BitPalette`** (**S8-stile, D20**: 4 indici — 0 trasparente, 1 riempimento, 2 **anello** spesso **R** px (2 emery / 1 flint), 3 **ombra 3D** profonda **S** = 2 px, solo emery: su flint la strip ha 3 colori, D26) generati da TTF con freetype-py (`tools/gen_digits.py`, `FT_LOAD_TARGET_MONO` come `fontgen.py`), una strip per (font, taglia, piattaforma), `gbitmap_create_as_sub_bitmap` per cifra, `GCompOpSet`; colore a runtime via `gbitmap_get_palette(b)`, con gli indici riconosciuti **dal colore** (0xFF riempimento, 0xC0 anello, 0xF0 ombra; spento = `GColorClear`): i quattro **stili cifre** di D21 sono solo tre byte di palette sulla stessa risorsa. **Entrambi i layout**; in più `FONT_KEY_LECO_60_BOLD_NUMBERS_AM_PM` (cifre alte 42 px, zero risorse) come 4ª opzione del layout A. | `.pbf`: limite hard **512 B/glifo** (area ≤ 4.096 px, build fallisce); PDC non pixel-exact; fctx toolchain 2016. Sprite pixel-exact, contorno **in un solo blit**, disegnabili anche su flint. | `03` F0–F5, F10–F13; `02` F19–F22 ✅ |
| D4 | **Font**: **Anton** (0, default), **Bebas Neue** (1), **Barlow Condensed Bold** (2), **LECO 60 di sistema** (3, solo layout A) e — **S8-stile, D22** — **Francois One** (4) e **Staatliches** (5) (tutti SIL OFL 1.1, TTF statici da `github.com/google/fonts`; `OFL.txt` in `resources/fonts/`). L'enum è stabile e l'indice della strip si ricava con `gal_font_strip(font)` (LECO non ha strip). Niente LECO 1976/Avenir come file (commerciali). | `03` F9, F14–F15 ✅; → `galleria-s8-stile.md` §2 |
| D5 | **SDK 4.33.1** (fw ≥ 4.32), superficie API ≤ 4.17; in S7 build di prova con 4.17; scelta prima di S9. | piano §2.4 |
| D6 | **Dithering Floyd–Steinberg serpentine** (opzioni Bayer 4×4 "compatto", nessuno); LUT di tono (luminosità/contrasto); **"Ottimizza per il vetro"** (quantizzazione nello spazio della LUT sunlight) **default OFF** finché S8 non la conferma sul pannello PT2 ❓; anteprima ×2 con la LUT di `pebble screenshot`. | Senza dithering la foto si posterizza (S0). Il dithering **non costa energia** (righe intere) ✅. LUT del pannello 2015. | `05` F3, F9–F12 |
| D7 | **Colore testo calcolato sull'orologio** sulla fascia delle cifre, una volta per foto/fascia: `LUM_SUN[64]` (o `LUM_RAW`), % pixel in conflitto per bianco (Y > 77) e nero (Y < 25), parità → media < 46 = bianco, isteresi 10 punti, **contorno** se conflitti > 15 % (auto/sempre/mai). Manuale: bianco, nero, giallo pastello (19:1 su nero), blu Oxford (16,6:1 su bianco). flint: maggioranza dei pixel, contorno sempre. **S2 (v1.3):** la fascia valutata è **tutta la fascia dinamica** `[0, band_h)` (copre cifre e riga info, stesso colore); campionamento 1 px su 2 (3.800 letture); isteresi solo se c'è un risultato precedente valido; contorno automatico calcolato sul colore **effettivo** (anche manuale: giallo → conflitti del bianco, Oxford → del nero); 1 bit: parità 50/50 → media 127 → nero; senza bitmap → bianco senza contorno. Contorno in S2 = 8 `draw_text` sfalsati di 1 px per le cifre, 4 "a croce" per la riga info, tratto 5 sotto l'icona BT e `gpath_draw_outline` sotto il fulmine (in S3 gli sprite lo fanno in un blit). Verificato: log `luma:` sull'orologio = `photo_prep.py --stats`. | < 1 ms; il telefono non conosce layout/Quick View attivi; `gcolor_legible_over()` decide per un solo colore. Scrim via `graphics_fill_rect` con alpha **non** blenda. | `05` F13–F15, §3 ✅ |
| D8 | **Persist** (§4.2): manifest unico (chiave 1, **234 B** dallo schema 2 del 04/09/2026 — slot + ordine + **impostazioni** + `shake_offset` riservato + CRC16; 214 B nello schema 1, ormai solo migrazione: §4.2, D18/D19) scritto **per ultimo**, chunk `1000 + k·256 + i`, **12 slot**, file ≈ 430 KB (allocazione 512 KiB); sostituzione = sovrascrittura delle stesse chiavi (mai `persist_delete` sui chunk); `persist_get_max_size()` ≥ 1 MiB altrimenti modalità demo. | 1 MiB ✅ ma ogni chiave **nuova** scansiona gli header (∝ dimensione file), crescita per raddoppio con riscrittura completa (watchdog sospeso 60 s), compattazione a ~20 % di spazio morto. PT2: **5–7 ms/`persist_write_data`**, 70 µs/lettura. | `02` F11–F18; `04` F11 ✅ |
| D9 | **Protocollo AppMessage** (§5). ~~Inbox a due fasi (256 B a riposo, 7.971 B in sync)~~ → **S5a: una sola inbox da 4.153 B (chunk 4.096 B = 16 chunk persist) aperta in `init()` e mai chiusa**; outbox **esatta** (**119 B** = HELLO da `dict_calc_buffer_size` dalla v1.9 con `OPEN_MS`; 110 B fino alla v1.8 — revisione 29/08): `app_message_close()` non è esportata dall'SDK 4.33.1 (né in `pebble.h` né in `libpebble.a`), quindi "chiudi → riapri" è impossibile; il chunk da 4 KB invece di 8 KB tiene il layout B sopra i 40 KB liberi. Un messaggio in volo; 16 chunk scritti in persist **dentro** `inbox_received` (l'ACK parte al ritorno → flow control gratuito); `STATUS` con codici; backoff PKJS 1-2-4-8-8-8-8 s; 30 s di silenzio → IDLE. | `app_message_close()` esiste solo nel firmware; `app_message_open()` fallisce con `APP_MSG_INVALID_STATE` se già aperta e non ritaglia la dimensione richiesta (PebbleOS `app_message.c`); ACK dopo il callback; il telefono attende 10 s ✅. Throughput BLE **mai misurato** (5–15 KB/s ⚠️). | `01` F10–F14; `07` §1.3; S5a |
| D10 | **Rotazione stateless**: slot = funzione del tempo (`t = now_min / intervallo`; sequenziale `order[t mod n]`, casuale permutazione LCG "mai la stessa due volte"; giornaliera = primo tick dopo le 04:00), nessuna scrittura persist; shake (`accel_tap_service`) = offset **solo in RAM**, mai persistito (D18 → **D19**, 04/09/2026: vale fino al riavvio della watchface; prima era scritto in `deinit` sulla chiave 2, ~100 record morti al giorno); **niente cambio foto fuori focus** (`app_focus_service`) **né durante una sync** (`model_sync_hold`, revisione 29/08: il manifest in RAM cambia già al `PHOTO_BEGIN`; tick/shake/focus si applicano al rilascio). Default: 30 min, sequenziale, shake on. | Coperta da notifica la watchface gira a 0,4 fps ✅; zero scritture a regime. | `04` F22; `07` §1.5 |
| D11 | **Ridisegno mirato**: un solo `Layer`; al tick solo la **fascia dinamica** (sub-bitmap della foto + sprite + riga info, y ≤ 106 in A; in B la riga MM + "PM", `y ≥ 119`); foto intera solo al cambio foto/layout/ora (in B); `window_set_background_color(GColorClear)`; un `layer_mark_dirty()` per tick; compositing riportato a `GCompOpAssign` prima del testo. Quick View (59 px emery / 51 flint): A invariante; B → riga singola con la strip A. | §7.7 ✅ |
| D12 | **Foto demo** (2) come risorse `raw` raw6/raw1, **una risorsa per foto letta con UNA sola `resource_load_byte_range` dall'offset 0** (ricerca 04 F12: il costo cresce con l'offset iniziale, O(offset) ≈ 2,8 MB/s di "seek" su PT2: 23 letture a blocchi sarebbero costate ≈ 0,15–0,25 s per foto). Il payload va nella coda del buffer del bitmap e si decodifica in place (garanzia in `photo_codec.h`; raw1 è già nel layout del bitmap): nessun buffer di staging. Album vuoto → demo. **S2:** `DEMO_{1,2}_RAW6` (emery) / `DEMO_{1,2}_RAW1` (flint) con `targetPlatforms` per risorsa; demo_1 `Big_Dipper` (scura → testo bianco), demo_2 `Snowy_Ubuntu_Light` (chiara → testo nero); **CC-BY-SA-4.0, solo test**: da sostituire con foto proprie/CC0 prima di S9 (`resources/photos/README.md`). Caricamento demo 1–3 ms in emulatore. | `04` F12 ✅ |
| D13 | Layout B **solo cifre** in v1; riga info sotto in v1.1 se richiesta. 12 foto a 64 colori (non 24 a 16 colori). iOS validato "best effort" in S8 (Android è il percorso certo). | risposte utente + brief §6 |
| D20 | **Quarto indice della palette 2 bit = ombra 3D e anello spesso R**: 0 trasparente, 1 riempimento, 2 anello (dilatazione di Chebyshev di R px meno il riempimento), 3 ombra (spostamenti diagonali (+k,+k), k = 1..S). **R = 2, S = 2 su emery; R = 1, S = 0 su flint**; PNG a 4 colori esatti (trasparente / bianco / nero / rosso). | Un solo blit per glifo e una sola risorsa per tutti gli stili: cambiano 3 byte di palette. Un `4BitPalette` raddoppierebbe l'heap delle strip (layout B sotto i 40 KB); l'ombra come blit sfalsato è impossibile con il riempimento trasparente. | → `galleria-s8-stile.md` D20 |
| D21 | **Impostazione `digit_style`** (byte 12 di `GalSettings`, ex `reserved[0]`): **0 pieno**, **1 trasparente** (riempimento `GColorClear`, anello = colore testo), **2 trasparente 3D** (+ ombra del colore opposto), **3 pieno 3D**. Con LECO lo stile è ignorato dall'orologio e forzato a 0 dalla pagina; negli stili trasparenti «Contorno» non ha effetto (l'anello c'è sempre) e la pagina lo disabilita. | I blob vecchi hanno 0 = pieno: nessuna migrazione, schema 1 invariato (20 B). | → `galleria-s8-stile.md` D21 |
| D22 | **Enum dei font stabile**: 0 Anton, 1 Bebas, 2 Barlow, 3 LECO, **4 Francois One, 5 Staatliches**; indice della strip = `gal_font_strip(font)` = `font < 3 ? font : font − 1`; tabelle generate `DIGITS_METRICS[5][2]` / `DIGITS_RESOURCE_IDS[5][2]`. | Le impostazioni già salvate (font 3 = LECO) restano valide; `settings_validate`: `font < 6`, `digit_style ≤ 3`. | → `galleria-s8-stile.md` D22 |
| D23 | **Anello di 2 px anche per lo stile pieno con «Contorno»** su emery (prima 1 px); su flint resta 1 px. | Un solo indice per l'anello: lo spessore è quello della strip. A 66/94 px il contorno da 1 px era troppo sottile sulle foto a righe. | → `galleria-s8-stile.md` D23 |
| D24 | **Pixel del riempimento invariati** per Anton/Bebas/Barlow: `pick_px`/`--fit-width` misurano ancora `riempimento + 2 ≤ cell_w` (e `':' + 2 ≤ cella del ':'`), **non** `+ 2R + S`; il generatore verifica invece che la riga peggiore stia nello schermo. | Altrimenti Anton (riempimento 35 px in A) e Barlow perderebbero altezza. | → `galleria-s8-stile.md` D24 |
| D25 (terza versione 05/09/2026) | **Griglia UNIFORME adattata al font**: `prv_grid_steps` calcola **un solo passo per tutte le cifre** — `max(cella, nucleo della cifra più larga + gap)`, nucleo = riempimento + 2R — e uno per il `:` — `max(cella del ':', nucleo del ':' + gap)`; `prv_place_row_fit` prova gap **2, 1, 0** in quest'ordine e tiene il primo per cui la riga (più gap 4 + «PM» 18 in 12 h) sta in `schermo − 2·FIT_MARGIN`, `FIT_MARGIN = 2`. In **riserva** `max(cella, riempimento più largo + R)`, sempre accettata e l'unica che il generatore garantisce con errore (anelli sovrapposti al massimo di 1 px, mai sul riempimento del vicino). La griglia dipende solo da font, taglia e numero di glifi: **le cifre non si spostano al cambio di minuto** (D3/S3). L'ombra può sporgere di 1 px nel margine interno del vicino (stili 3D) e il blocco centrato di ≤ R a sinistra e ≤ R + S a destra (clip del layer). | Con `max(passo, riempimento + R)` (2ª versione) gli anelli di due cifre larghe si toccavano: segnalazione dell'utente «con Francois One in stile trasparente le cifre si toccano», vera con tutti i font tranne Bebas Neue; la 3ª bis, con passi **proporzionali al singolo glifo**, è stata scartata perché il blocco centrato spostava le ore di ±4 px al cambio di minuto. In 24 h ci sta il gap 2 per tutti — Anton porta la griglia da 40 a **41** px (e il `:` da 16 a 19): riga **183**, fino a 195 con Barlow — mentre in 12 h con «PM» si scende a gap 0 (Anton 195, Bebas 194) o alla riserva (Barlow, Francois One, Staatliches: griglia 40, riga 198), §3.1. Con `max(passo, inchiostro)` la riga 12 h in A sarebbe 201–208 px > 200. | → `galleria-s8-stile.md` D25 |
| D26 | **Su flint niente ombra (S = 0): strip a 3 colori** (`strip_h` 44/64 come prima); gli stili 2 e 3 valgono come 1 e 0 sull'orologio e la config page disabilita le due opzioni 3D normalizzando 2 → 1, 3 → 0. | Nel `.pbi` di una piattaforma B/N la SDK passa ogni pixel per `nearest_color_to_pebble2_palette`: restano 0x00/0xC0/0xFF, il rosso diventa nero e l'ombra si fonde con l'anello (verificato in emulatore il 04/09/2026). | → `galleria-s8-stile.md` D26 |
| D27 (05/09/2026, revisione v1.9) | **Soglia dell'avviso «Galleria si avvia lentamente» proporzionale alle foto**: `400 + 100 × n` ms, n = slot validi nello snapshot dell'HELLO (`page_core.js slowThresholdMs`), invece di 1.000 ms fissi. | `OPEN_MS` = apertura del file (2 scansioni) **+ ricerca della chiave 0**, che una migrazione 1 → 2 riscrive in coda (fino a una scansione in più): 12 foto sane 0,65–1,2 s, 4 foto su file gonfio 2,1 s (misure 04/09, §5 «HELLO, v1.9»). Da confermare con l'album pieno (PIANO §7). |
| D28 (05/09/2026) | **Heap del layout B: accettati 38,7–40 KB liberi** con i font nuovi (Francois One 38.724, Staatliches ≈ 39,9 K, Anton 40.452 dopo il primo render); O1 (≥ 40 KB) resta la soglia di guardia per i font vecchi. La config page **descrive il limite di 12 foto** (contatore «ora N su 12» e costo all'avvio ≈ 0,1 s per foto). | Decisione dell'utente: le alternative (S = 1 in B, `DigitInk` packed, chunk 2.048) recuperano < 300 B o raddoppiano i messaggi per foto; a regime nessuna allocazione oltre la strip A della Quick View (~7,3 KB). |

Contraddizioni risolte: PNG8 (report 05) e PBI 4-bit (report 04) scartati per raw6 (report 02); LECO 60 come default (report 03) scartato per le cifre da 42 px; la config page con `localStorage` (design v1.0) corretta dal brief (origine opaca).

## 3. Wireframe

### 3.1 emery 200×228 — Layout A "un terzo" (default)
```
x:  0        12                          188      200
y 0 ┌─────────────────────────────────────────────┐
    │ foto raw6 a schermo intero (GCompOpAssign)   │
y 7 │  ┌────┬────┬──┬────┬────┐                    │ celle 40|40|16|40|40 = 176 px, x0 = 12; strip 72 righe da y 7 (S8-stile)
    │  │ 2  │ 3  │: │ 5  │ 9  │  [AM]              │ riempimento 66 px da y 9 = palette[1], anello 2 px = palette[2], ombra 2 px = palette[3]
y 79│  └────┴────┴──┴────┴────┘                    │ (LECO 60: box (0,6,200,60), cifre 42 px, contorno con 4–8 draw_text sfalsati)
y 82│   6.532 ▪   82 % ▪   mer 26 ago              │ riga info Gothic 18 Bold (24 se ExtraLarge), 3 celle x 4|68|132, w 64, h 22
y106│ ─ ─ ─ ─ ─ fine fascia dinamica ─ ─ ─ ─ ─ ─   │ sotto: nulla cambia al tick
y169│ ┈┈┈┈┈┈┈ Quick View 59 px (se attivo) ┈┈┈┈┈┈ │ nessun elemento nostro qui
y228└─────────────────────────────────────────────┘
```
Riga info: passi (`health_service_sum_today(HealthMetricStepCount)` se `health_service_metric_accessible`), batteria (`battery_state_service_peek`, "⚡" in carica), data (`strftime "%a %e %b"`, `setlocale(LC_ALL,"")`); icona BT barrata al posto dei passi se disconnesso (nessuna vibrazione); "Foto 3/12" durante la sync. 12 h: "AM/PM" Gothic 14 Bold a destra delle cifre.
**S1 (v1.2):** la riga info non usa 3 celle fisse da 64 px ("Wed 26 Aug" in Gothic 18 Bold ≈ 85 px): passi/BT allineati a sinistra (x 4), data a destra (fino a x 196), batteria centrata nello spazio fra i due; larghezze misurate al tick e memorizzate; data accorciata "mer 26" → "26" se non ci sta. Font riga info: Gothic 18 Bold (24 Bold con content size ExtraLarge → riga y 80 h 28, fascia 108). LECO 60 Bold in box (0,6,200,60) rende le cifre a **y 24–65** (42 px, a filo del fondo del box, 18 px vuoti sopra); flint LECO 42 in box (0,6,144,44) → **y 19–47** (29 px); misurato sugli screenshot. AM/PM (Gothic 14 Bold) con la base sulla riga del fondo delle cifre. Fascia dinamica = riga info + 2 px: 106 emery (110 ExtraLarge), 76 flint. **S3 (v1.4), deciso:** in 12 h le celle delle **cifre** si stringono di 2 px, i due punti tengono la cella piena (**38|38|16|38|38 = 168**; flint 26|26|12|26|26 = 116: su emery il `:` di Anton/Barlow è largo 15 px e non entrerebbe in 14, su flint il `:` di Barlow è 12 px e non entrerebbe in 10) e il blocco cifre + 4 px + AM/PM (Gothic 14 Bold, base allineata al fondo reale delle cifre, `digit_h`) è centrato: "HH:MM AM" = 168 + 4 + 18 = 190 → x0 = 5; "H:MM AM" = 130 + 22 → x0 = 24; flint 116 + 22 = 138 → x0 = 3 (griglia **nominale**: con l'anello di S8-stile la griglia si allarga, vedi D25 qui sotto). Barlow Condensed Bold ha il `4` largo 42 px a 66 px di altezza (le altre cifre 35–37): in taglia A il generatore lo riduce con `--fit-width` (61 px su emery, 40 su flint, `digit_h` reale nelle metriche); con l'anello di S8-stile la griglia della taglia A resta **uniforme** ma si **adatta al font** (**D25 terza versione**, 05/09/2026, dopo la segnalazione dell'utente «con Francois One in stile trasparente le cifre si toccano»): `prv_grid_steps` dà un solo passo a tutte le cifre — `max(cella, nucleo della cifra più larga + gap)`, nucleo = riempimento + 2R — e uno al `:` — `max(cella del ':', nucleo del ':' + gap)` — con il gap provato 2, 1, 0 e accettato se la riga (più 4 + «PM» 18 in 12 h) sta in `schermo − 2 × 2 px`; in riserva `max(cella, riempimento più largo + R)`, sempre accettata. Dipendendo solo da font, taglia e numero di glifi, **le cifre non si spostano al cambio di minuto** (D3/S3: la variante con passi per singolo glifo spostava le ore di ±4 px). In **24 h** ci sta sempre il gap 2 — su emery il passo vale l'inchiostro della cifra più larga, `S = 2` — quindi la griglia piena resta solo dove nessun inchiostro la supera: «HH:MM» = 180 px con Bebas, 183 con Anton (griglia 41, `:` 19 → x0 = 8), 192 Staatliches, 193 Francois One, 195 Barlow (griglia 44, `:` 19 → x0 = 2); su flint da 124 (Anton, Bebas) a 134 (Barlow). In **12 h**, dove ai 200 px vanno tolti gap 4 + «PM» 18 e i 2 px liberi per lato, si scende di gap: «HH:MM PM» = 194 px con Bebas e 195 con Anton (gap 0), 198 con Barlow, Francois One e Staatliches, che ricadono sulla **riserva** (griglia 40: solo qui gli anelli di due cifre larghe tornano a toccarsi); su flint 138 (Anton, Bebas, Staatliches) e 142 (Barlow, Francois One), dentro i 144. «09:44 PM» misura quanto «10:44 PM»: cambiano solo i margini. Le strip sono `resources/digits/<font>_<taglia>~color.png` / `~bw.png` (**S8-stile**: PNG a 4 colori — trasparente / bianco = riempimento / nero = anello spesso R / rosso = ombra 3D profonda S; 3 colori su flint, D26; la palette del `.pbi` ha ordine arbitrario → `ui_digits` riconosce gli indici dal colore; **S7**: strip **compatte** — 11 glifi adiacenti in A, 10 in B senza `:` — con `cell_w` nell'header = passo della griglia del layout), generate da `tools/gen_digits.py --fit-width --no-colon-b --pack` con le metriche in `src/c/digit_metrics.h`; ogni glifo è una sub-bitmap del solo inchiostro (`ink[].x/.w`), centrato nella cella del layout al disegno. Screenshot: `docs/design/galleria/s1_*.png`, `s3_*.png`.

### 3.2 emery — Layout B "tutto schermo"
```
y 11 ┌ H1 (32,11,64,100) ┐ ┌ H2 (104,11,64,100) ┐   strip 100 righe (riempimento 94 px da y 13), celle 64 px, gap 8, blocco 136 px centrato
y111 └───────────────────┘ └────────────────────┘
y119 ┌ M1 (32,119,64,100)┐ ┌ M2 (104,119,64,100)┐   riempimento MM da y 121, margine inferiore 12 px sotto il riempimento
y219 └───────────────────┘ └────────────────────┘   12 h: "PM" Gothic 14 Bold in (170,214,28,14); fascia dinamica da y 118
```
Zero iniziale off → H2 in (68,11,64,100). Quick View attivo (h = 169) → una riga "HH:MM" con la strip A (72 righe) a y 7, senza riga info. **S3:** nel layout B la strip A restava caricata insieme alla B (heap 7.932 + 17.568 B; pbpack 7.496 + 16.912) per passare alla riga singola senza allocazioni a ogni peek (`ui_time_unobstructed_changed` → `ui_time_band_changed`, isteresi di luma); **S7 (D16): la strip A viene caricata solo per la durata della Quick View** (`prv_load_strips` da `prv_refresh_mode`: `need_a = (A e font sprite) || (B, B caricata e area non ostruita < schermo)`, stesso font della B; allocazione al `did_change` che apre il peek — una `gbitmap_create_with_resource` da ≈ 7,5 KB di flash, evento raro e mai in `update_proc` — e `unload` al `did_change` che lo chiude: −8.152 B di heap a regime in B; A non caricabile → LECO; la strip B è a **10 celle** (senza `:`, `gen_digits.py --no-colon-b`): −1.536 B di heap e −4.608 B di pbpack su emery); **fascia dinamica in B = dalla riga MM al fondo** (`[strip_y(MM) − 1, 228)` = **`[118, 228)`** dopo S8-stile: minuti + "PM"), la riga HH e il cambio AM/PM si ridisegnano con un redraw completo quando cambiano le ore (confronto con l'ultimo HH, quindi anche su salti DST); luma valuta tutto lo schermo; riga singola in Quick View: fascia `7 + 72 + 2 = 81` px (S8-stile); 12 h: "PM" in basso a destra con la base a 2 px dal bordo; font LECO in B → Anton; senza strip A sotto Quick View → LECO.

### 3.3 flint 144×168 (1 bit)
```
Layout A: celle 28|28|12|28|28 = 124 px, x0 = 10, strip 44 righe a y 6..49 (riempimento 42 px da y 7, anello 1 px, nessuna ombra — D26; o LECO 42, cifre 29 px); riga info Gothic 14 a y 56 (h 18); fascia dinamica y ≤ 76
Layout B: strip 64 righe — H1 (20,12,48,64) H2 (76,12,48,64) · M1 (20,92,48,64) M2 (76,92,48,64), riempimento da y 13 e y 93 (invariati rispetto a S7); Quick View 51 px (h = 117) → riga singola A
Foto 1BitPalette FS (opzione Atkinson) con LUT di tono; contorno sempre; colore = maggioranza dei pixel nella fascia
```

## 4. Modello dati

### 4.1 Struct C (tutte `packed`, validate con `persist_get_size() == sizeof`)
```c
#define GAL_MAX_SLOTS      12
#define GAL_KEYS_PER_SLOT  256
#define GAL_KEY_SCHEMA     0
#define GAL_KEY_MANIFEST   1
#define GAL_KEY_ROTSTATE   2
#define GAL_KEY_SETTINGS   10
#define GAL_KEY_CHUNK(k,i) (1000u + (uint32_t)(k) * GAL_KEYS_PER_SLOT + (i))
#define GAL_MAGIC          0x314C4147u          /* 'GAL1' */
enum { PHOTO_FMT_NONE = 0, PHOTO_FMT_RAW6_200x228 = 1, PHOTO_FMT_RAW1_144x168 = 2 };

typedef struct __attribute__((packed)) {
  uint8_t  state;        /* 0 vuoto, 1 valido */
  uint8_t  format;       /* PHOTO_FMT_* */
  uint16_t generation;   /* +1 a ogni sostituzione */
  uint32_t length;       /* 34200 | 3024 */
  uint32_t crc32;        /* sul payload */
  uint32_t photo_id;     /* assegnato dal telefono (≠ 0) */
} GalSlotMeta;           /* 16 B */

typedef struct __attribute__((packed)) {                    /* SCHEMA 2 (revisione perf 04/09/2026) */
  uint32_t    magic;  uint8_t schema;  uint8_t slot_count;   /* GAL_MAGIC, 2, 12 */
  uint8_t     order[GAL_MAX_SLOTS];                          /* indici slot in ordine di rotazione, 0xFF = fine */
  uint16_t    shake_offset;                                  /* ex reserved: riservato, l'app non lo legge né lo aggiorna (D19); 0 su file nuovo, su file migrato conserva l'ultimo valore della chiave 2 */
  GalSlotMeta slots[GAL_MAX_SLOTS];                          /* 192 B */
  GalSettings settings;                                      /* 20 B (schema 1: chiave 10 separata) */
  uint16_t    crc16;
} GalManifest;                                               /* 234 B, chiave 1, scritto PER ULTIMO; schema 1 = 214 B (GalManifestV1, solo migrazione) */

typedef struct __attribute__((packed)) {
  uint8_t  schema;        /* 1 */
  uint8_t  layout;        /* 0 A, 1 B */
  uint8_t  font;          /* 0 Anton, 1 Bebas Neue, 2 Barlow Condensed, 3 LECO sistema (solo A), 4 Francois One, 5 Staatliches (S8/D22) */
  uint8_t  clock_mode;    /* 0 auto (clock_is_24h_style), 1 12 h, 2 24 h */
  uint8_t  leading_zero;  /* 0 auto (on in 24 h, off in 12 h), 1 on, 2 off */
  uint8_t  text_color;    /* 0 auto, 1 bianco, 2 nero, 3 giallo pastello, 4 blu Oxford */
  uint8_t  outline;       /* 0 auto (> 15 % conflitti), 1 sempre, 2 mai */
  uint16_t interval_min;  /* 0 mai; 5, 15, 30, 60, 180, 1440 */
  uint8_t  order;         /* 0 sequenziale, 1 casuale */
  uint8_t  shake_next;    /* 0/1 */
  uint8_t  info_row;      /* bit0 passi, bit1 batteria, bit2 data, bit3 BT */
  uint8_t  digit_style;   /* S8/D21: 0 pieno, 1 trasparente, 2 trasparente 3D, 3 pieno 3D (ex reserved[0]: i blob vecchi valgono 0) */
  uint8_t  reserved[5];   /* v1.2 (S1): con [4] la struct era 18 B, non 20 */
  uint16_t crc16;
} GalSettings;            /* 20 B, dentro il manifest (schema 1: chiave 10) (_Static_assert in settings.c) */

typedef struct __attribute__((packed)) { uint16_t shake_offset; uint16_t crc16; } GalRotState; /* 4 B: schema 1 chiave 2 (solo migrazione); schema 2 = campo del manifest, API di storage invariata; S4: shake a 16 bit modulo 27.720 = mcm(1..12) (con 8 bit il wrap 255→0 annullava una scossa per n | 255); manual_pos rimosso */
typedef struct { bool active; uint8_t slot, format; uint32_t photo_id, length, crc_expected, crc_running, next_offset; } GalPending; /* sync, solo RAM */
```
Moduli puri (senza `pebble.h`, test host in `apps/galleria/test/`): `photo_codec.h` (**S2**: `Raw6Unpacker` + `raw6_unpack_init/raw6_unpack` a streaming con carry 0–2 B che scrive `0xC0|idx`; `PhotoDecoder` con `photo_decoder_init(fmt, dst, stride)/feed/complete` che lavora per righe di input da 150/18 B e scrive con lo stride del bitmap; raw1 = copia; `raw6_pack` per test e tool), `crc.h` (**S2**: `crc32_update` zlib e `crc16_ccitt_update/crc16_ccitt` CCITT-FALSE, tabelle a nibble 96 B), `luma.h` (**S2**: `luma_compute_8bit/1bit(data, stride, LumaRect, LumaResult*)`, `LUMA_SUN[64]`, `luma_reset`), `timefmt.h` (`timefmt_hhmm`, `timefmt_ampm`), `rotation.h` (**S4**: `rotation_local_minutes(year, month, day, hour, min)` = minuti locali dal 1970 via `days_from_civil`; `rotation_index(now_min, n, interval_min, order, shake_offset)`; `rotation_sequence(manifest, native_format, skip_mask, out[12])` = `order[]` filtrato (VALID + formato nativo + non saltato + senza duplicati, fallback 0..11); `rotation_slot(now_min, manifest, native_format, skip_mask, interval_min, order, shake_offset)` → slot o `GAL_SLOT_NONE`), `gal_types.h` (**S4**: `GalSlotMeta`/`GalManifest`/`GalRotState`, chiavi, `GAL_MAGIC`, `_Static_assert` sulle dimensioni; header puro condiviso da `storage.c`, `rotation.c`, `model.c` e dai test).

### 4.2 Persist (chiavi)
```
0        uint32 schema (= 2)
1        GalManifest (234 B)   ← scritto per ultimo nella sync: manifest valido ⇒ foto complete; porta anche le impostazioni (`shake_offset` riservato: l'app non lo legge né lo aggiorna — D19 —; 0 su file nuovo, sul file migrato resta congelato l'ultimo valore della chiave 2)
2        schema 1: GalRotState (4 B)   ← letta UNA volta dalla migrazione, mai più scritta né cancellata
3–9      riservate
10       schema 1: GalSettings (20 B)  ← idem
11–99    riservate
1000 + k·256 + i   chunk i (256 B) dello slot k (k 0..11, i 0..255 → 64 KiB/slot; raw6 usa 134, raw1 12)
```
**Perché un solo record di metadati (revisione perf 04/09/2026, misure S8 + sorgente PebbleOS 4.36.2 `settings_file.c`/`pfs.c`)**: il firmware apre il file persist senza `OP_FLAG_USE_PAGE_CACHE` e ogni ricerca di chiave (`search_forward`) è una scansione lineare dei record in cui ogni passo ricammina la catena di pagine PFS da 4 KB; con 12 foto raw6 (~430 KB, ≥ 1.600 record) una scansione costa ~0,4 s (quadratica nel file). L'apertura è pigra (prima chiamata persist: `bootup_check` + `compute_stats` = 2 scansioni in una sola syscall, ~0,8 s, firmware), ogni chiave assente costa una scansione a vuoto (la chiave 10 non è mai esistita sul campo: `settings: defaults`), ogni scrittura di chiave nuova una scansione (12ª foto ≈ 55 s), ogni scrittura in `deinit` ritardava il launcher (~0,4–0,8 s dopo uno shake). Regole nuove: **una sola ricerca di metadati** all'avvio (manifest), **mai chiavi che possono mancare**, **nessuna scrittura persist in deinit** (solo impostazioni pendenti via `storage_flush`), **lo shake non si persiste** (solo RAM, vale fino al riavvio della watchface: D10 rivista — il tap service scatta ~100 volte al giorno e ogni record in più è un passo in più in ogni scansione), `s_schema_ok` alzato in `storage_init` (la prima scrittura non ricerca la chiave 0), `settings_apply` con impostazioni identiche non scrive (nessun record morto a ogni HELLO). Migrazione 1 → 2 in `storage_init`: manifest da 214 B valido → slot/ordine copiati, impostazioni dalla chiave 10 e shake dalla chiave 2 se presenti e valide (due ricerche, una volta sola), record nuovo scritto dal timer/flush, chiave 0 riscritta = 2 alla prima scrittura (riscrivere = **appendere in coda**: da lì in poi la ricerca della chiave 0 dentro `open` costa fino a una scansione in più, mentre quella del manifest diventa quasi gratis — F04, 05/09); le chiavi 2/10 restano nel file (cancellarle costerebbe una scansione ciascuna). Su flint (raw1) il file è ~37 KB: costi ~10× minori.
Regole: chunk in ordine crescente di `i` → manifest; avvio: `persist_exists` + `persist_get_size == sizeof` + magic + CRC16 altrimenti default (mai crash; schema futuro → reset); caricamento foto: 134 letture in ordine senza intercalare altre chiavi, CRC32 accumulato, `layer_mark_dirty` solo se coincide (altrimenti slot invalido in RAM → successivo); sostituzione = stesse chiavi (≈ 36 KB di spazio morto per foto sostituita → compattazione dopo ~3 sostituzioni: secondi ❓, coperti dal backoff PKJS); `#if PBL_API_EXISTS(persist_get_max_size)` e valore ≥ 1.048.576 altrimenti modalità demo; `E_OUT_OF_STORAGE` sempre gestito. Occupazione: 12 × 35.808 + 238 ≈ **430 KB** (41 %; allocazione 512 KiB); flint ≈ 38 KB. Se in S8 il costo per chiave nuova risultasse ~1 s, scendere a 8 slot.
**S4 (`storage.c`)**: API `storage_init()` (quota, schema → reset se futuro, manifest validato) · `storage_manifest()`/`storage_valid_slots()` · `storage_read_settings()`/`storage_settings_changed()` (nel manifest; AppTimer 10 s condiviso con lo shake, `app_timer_reschedule` se già pendente) /`storage_flush()` (deinit: solo impostazioni pendenti) · `storage_read_rotstate()`/`storage_write_rotstate()` (campo del manifest, non più usato dall'app: lo shake resta in RAM — perf 04/09) · `storage_read_chunk(slot, i, buf, cap)` · `storage_write_chunk(slot, i, data, n ≤ 256)` → `storage_commit_slot(slot, format, length, crc32, photo_id)` (state VALID, `generation+1`, slot aggiunto in coda a `order[]` se assente, manifest scritto; su errore la copia RAM viene ripristinata) · `storage_set_order()`/`storage_clear_slot()` (S5a; entrambe **idempotenti**: ordine identico o slot già EMPTY ⇒ `STORAGE_OK` senza scrivere, revisione 05/09). Con l'album disabilitato (quota < 1 MiB) il manifest scritto conserva slot e ordine letti dal file (copia `s_backup`), mentre in RAM restano azzerati. Codici `StorageResult` OK/ERR/NO_SPACE/DISABLED. ⚠️ `persist_write_int`/`persist_write_data` ritornano i **byte scritti** in caso di successo (non `S_SUCCESS`): controllare `< 0`. Lettura foto: `ui_photo_load_persist(slot, meta)` con buffer static da 256 B → `PhotoDecoder` a streaming → CRC32 (emulatore: 8–13 ms per 134 chunk su emery, 1 ms su flint); scrittura di 134 chunk + manifest in emulatore ≈ 1,0–1,2 s con crescita del file, 145 ms sovrascrivendo. Testabile su host con lo shim `test/shim/pebble.h` (persist in memoria, quota, `E_OUT_OF_STORAGE` iniettabile, timer).

### 4.3 Formato raw6 (emery)
`idx = (r2<<4)|(g2<<2)|b2` (RGB 0/85/170/255 esatti: `GColorFromRGB` **tronca** `>>6`). Riga-maggiore, 4 px → 3 B: `b0 = p0<<2 | p1>>4; b1 = (p1&15)<<4 | p2>>2; b2 = (p2&3)<<6 | p3`. Riga 150 B, totale 34.200 B. Byte del `GBitmap` 8Bit = `0xC0 | idx`.
### 4.4 Formato raw1 (flint)
`GBitmapFormat1BitPalette` 144×168: MSB-first (pixel 0 nel bit 0x80), riga 18 B, 3.024 B, palette `static {GColorBlack, GColorWhite}` (`gbitmap_create_blank_with_palette`, `free_on_destroy=false`). ⚠️ Non `GBitmapFormat1Bit` (LSB-first, stride 20 B).

## 5. Protocollo telefono ↔ orologio (S5)

> **v1.9 (04/09/2026, revisione perf: `HELLO.OPEN_MS` = tempo di apertura del file persist, 17ª messageKey, outbox 119 B); v1.8 (29/08/2026, revisione post code-review: `SYNC_REQUEST.OFFSET`, outbox esatto, rotazione congelata in sync, attesa dell'HELLO, classi di retry — nessuna messageKey nuova); v1.7 (S5b: `HELLO.CRC`, lato telefono in §5.1); v1.6 (S5a)** — sezione riscritta sul codice reale (`sync_proto.c` puro + `sync.c` glue + `src/pkjs/sync.js`). **Deviazione da D9**: `app_message_close()` **non è esportata dall'SDK 4.33.1** (assente da `pebble.h` e da `libpebble.a`: esiste solo dentro il firmware) e `app_message_open()` ritorna `APP_MSG_INVALID_STATE` se già aperta ⇒ nessuna "inbox a due fasi": **una sola inbox**, aperta in `init()` e mai chiusa. Per non pagare 8 KB di heap a regime il chunk è **4.096 B** (16 chunk persist per messaggio): inbox `dict_calc_buffer_size(4, 4,4,4,0) + 16 + 4.096 = 4.153 B` su emery (9 messaggi per foto raw6), `3.129 B` su flint (chunk 3.072 ≥ 3.024: un messaggio per foto). `app_message_open()` **non ritaglia** la dimensione richiesta al massimo del telefono (PebbleOS `app_message.c`, letto il 29/08/2026): si può aprire anche senza telefono connesso; il chunk annunciato viene rinegoziato a ogni `JS_READY` da `app_message_inbox_size_maximum()`.

**messageKeys** (17, `package.json`, in quest'ordine → `MESSAGE_KEY_*` = 10000…10016, variabili `uint32_t` runtime, non costanti): `MSG, PROTO, MAX_CHUNK, SLOTS, COUNT, SLOT, PHOTO_ID, FORMAT, LENGTH, CRC, OFFSET, DATA, CODE, ORDER, SETTINGS, REPLY_TO, OPEN_MS` (`REPLY_TO` da S5b, `OPEN_MS` da v1.9; dopo un cambio serve `pebble clean`).
`MSG` (`SyncMsg`): 1 JS_READY · 2 HELLO · 3 SYNC_REQUEST · 4 SYNC_READY · 5 PHOTO_BEGIN · 6 PHOTO_DATA · 7 PHOTO_END · 8 STATUS · 9 SYNC_DONE · 10 SETTINGS · 11 ALBUM_ORDER · 12 ALBUM_DELETE.
`CODE` (`SyncCode`): 0 OK · 1 CRC_ERR · 2 NO_SPACE · 3 BAD_FORMAT · 4 BUSY · 5 SEQ_ERR · 6 NOT_SUPPORTED · **7 STORAGE_ERR** (S5a: errore persist ≠ `E_OUT_OF_STORAGE`).

**Tipi sul filo.** Il JS serializza ogni `Number` come **int32** (doc SDK "Number → int32"): l'orologio legge gli interi in base a `Tuple.length` (1/2/4 B, `prv_tuple_u32`) e gli `uint8` saturano a 255 (256 non deve diventare lo slot 0); `CRC`/`PHOTO_ID`/`LENGTH`/`OFFSET` vanno inviati come `x | 0` (bit pattern a 32 bit, riletto come `uint32`). Gli array JS diventano `TUPLE_BYTE_ARRAY`. L'orologio scrive `dict_write_uint8/16/32`. I dizionari con più chiavi possono arrivare in qualsiasi ordine; le chiavi assenti sono "campo mancante" (`SyncIn.fields`).

**Orologio → telefono** (outbox **esatta**: 119 B = `dict_calc_buffer_size(7, 1,1,1,2,2,2, SYNC_SLOTS_BYTES)` calcolata in `sync_init` (110 B fino alla v1.8, prima di `OPEN_MS`); HELLO è per costruzione il messaggio più grande — STATUS 44 B, SYNC_READY 18 B. Gli esiti delle `dict_write_*` sono accumulati: un messaggio incompleto lascia il WARNING `sync: dict_write -> %d, msg %u incompleto` (tripwire per l'emulatore, hook `GALLERIA_DEBUG_OUTBOX=100`) ma parte comunque, perché dopo `outbox_begin` il firmware è in OUT_WRITING e se ne esce solo con `outbox_send` — revisione 29/08, F4):
- `HELLO{MSG 2, PROTO u8 = 1, FORMAT u8, MAX_CHUNK u16, CRC u16, OPEN_MS u16, SLOTS 60 B}` — `FORMAT` = `PHOTO_FMT_*` nativo (1 raw6 emery, 2 raw1 flint: il telefono non indovina dalla piattaforma); `CRC` = CRC-16/CCITT-FALSE dei primi 18 B delle impostazioni correnti (v1.7, S5b: vedi sotto); `SLOTS` = 12 × `{state u8 (0/1), crc32 u32 LE}` dal manifest; `MAX_CHUNK 0` = trasferimento foto non supportato (album disabilitato per quota < 1 MiB, o telefono senza 8k); **`OPEN_MS`** (v1.9) = ms della **prima chiamata persist** = apertura del file da parte del firmware **più** la ricerca della chiave 0 (`storage_open_ms()`; **misurato sempre**: 0 = apertura sotto il millisecondo, campo assente = orologio pre-v1.9). 119 B = outbox.
- `SYNC_READY{MSG 4, MAX_CHUNK u16}` — valore effettivo (può essere rinegoziato).
- `STATUS{MSG 8, CODE u8, SLOT u8 (0xFF se non pertinente), OFFSET u32, REPLY_TO u8}` — `OFFSET` = prossimo offset atteso per la foto in corso (0 se nessuna); **`REPLY_TO`** (S5b, v1.7) = `MSG` del messaggio a cui lo STATUS risponde: il telefono, mentre attende la risposta a un messaggio preciso, **scarta gli STATUS di altri messaggi** (le copie ritrasmesse di `SETTINGS`/`ALBUM_ORDER` hanno lo stesso `SLOT 0xFF` e senza `REPLY_TO` la seconda risposta veniva letta come esito del passo successivo — revisione S5b). 44 B.

**Telefono → orologio** (inbox 4.153 B):
| Messaggio | Stato | Effetto / risposta |
|---|---|---|
| `JS_READY{MSG 1}` | qualsiasi | rinegozia il chunk, risponde `HELLO` |
| `SYNC_REQUEST{COUNT u8, OFFSET u32 opz.}` | qualsiasi | album disabilitato o chunk 0 → `STATUS{NOT_SUPPORTED}`; altrimenti → stato **SYNCING** (idempotente: foto in corso abbandonata; "Foto k/n" riparte da k = `OFFSET` = foto già concluse, clampato a `COUNT − 1`, 0 se assente — v1.8, F3), `SYNC_READY{MAX_CHUNK}`, timer di silenzio 30 s armato |
| `PHOTO_BEGIN{SLOT, PHOTO_ID, FORMAT, LENGTH, CRC, OFFSET}` | SYNCING (altrimenti `BUSY`) | campo mancante, `SLOT ≥ 12`, `FORMAT ≠ nativo`, `LENGTH ≠ photo_format_length`, `PHOTO_ID 0` → `BAD_FORMAT`; stima `slot_dopo × (LENGTH + ⌈LENGTH/256⌉ × 16) > 75 % di persist_get_max_size()` → `NO_SPACE`; **ripresa** se `SLOT`/`PHOTO_ID`/`CRC` coincidono con il `pending` attivo e `OFFSET == pending.next` (anche `OFFSET == 0`: ritrasmissione di un `PHOTO_BEGIN` il cui `STATUS` si è perso) — CRC parziale conservato e "Foto k/n" fermo; altrimenti nuovo `pending` da 0: **se lo slot era VALID viene prima svuotato nel manifest** (`storage_clear_slot`: un'interruzione lascia uno slot vuoto che la rotazione salta e che `HELLO` annuncia state 0, mai un manifest che certifica un payload misto) e "Foto k/n" avanza solo se la foto (slot + photo_id) è diversa dall'ultima conteggiata; risposta **sempre** `STATUS{OK, SLOT, OFFSET = pending.next}`: il telefono manda i dati da lì |
| `PHOTO_DATA{SLOT, OFFSET, DATA}` | SYNCING (altrimenti `BUSY`) | senza `pending` → `SEQ_ERR{OFFSET 0}`; slot diverso → `SEQ_ERR{next}`; `OFFSET < next` → **duplicato: nessuna risposta, nessuna scrittura**; `OFFSET > next`, `OFFSET % 256 ≠ 0`, `n = 0`, `n > MAX_CHUNK`, `OFFSET + n > LENGTH`, `n % 256 ≠ 0` senza essere l'ultimo pezzo → `SEQ_ERR{next}`; altrimenti `⌈n/256⌉` × `storage_write_chunk(slot, OFFSET/256 + j, …)` **direttamente dal buffer dell'inbox** (nessuna copia; l'ACK parte al ritorno del callback ⇒ flash scritto prima del chunk successivo), CRC32 accumulato, `next += n`, **nessuna risposta** (basta l'ACK); `E_OUT_OF_STORAGE` → `NO_SPACE`, altro errore → `STORAGE_ERR`, in entrambi i casi `pending` abbandonato |
| `PHOTO_END{SLOT, PHOTO_ID}` | SYNCING (altrimenti `BUSY`) | senza `pending`: se lo slot è VALID con lo stesso `PHOTO_ID` → `STATUS{OK, OFFSET = LENGTH}` (ritrasmissione dopo un commit il cui STATUS si è perso: idempotente), altrimenti `SEQ_ERR{0}`; `next ≠ LENGTH` → `SEQ_ERR{next}`; CRC diverso → `CRC_ERR` (manifest **non toccato**: lo slot resta vuoto se era una sostituzione, `pending` abbandonato); CRC uguale → `storage_commit_slot` (state VALID, generation + 1, `order[]`, **manifest per ultimo**) → `STATUS{OK, SLOT, OFFSET = LENGTH}`; commit fallito → `NO_SPACE`/`STORAGE_ERR` (copia RAM del manifest ripristinata). Il modello (`model_album_changed`: ricarica + redraw) viene avvisato **una volta a fine sync** (SYNC_DONE o timeout), non nel callback di ogni END. Durante la sync la rotazione è **congelata** (`model_sync_hold(true)` dopo ogni messaggio in SYNCING): tick, shake, focus e impostazioni aggiornano solo lo stato del modello e vengono applicati al rilascio (`model_album_changed` se il manifest è cambiato, altrimenti `model_sync_hold(false)` → log `rot(sync)`), come fuori focus (D10). Prima un tick a metà sostituzione leggeva il manifest in RAM con lo slot già svuotato e mostrava un'altra foto o la demo (revisione 29/08, F1) |
| `SYNC_DONE{MSG 9}` | SYNCING | → IDLE, "Foto k/n" spento, timer spento; **nessuna risposta**; in IDLE ignorato |
| `SETTINGS{SETTINGS 20 B}` | qualsiasi | blob = `GalSettings` (schema 1, campi in range, `crc16` ignorato: lo ricalcola `storage`); `settings_apply` → `sync_env_settings_changed(prima)` → UI: `ui_time_tick` se cambiano 12/24 h, zero iniziale, riga info; `ui_time_layout_changed` se layout/font; `ui_time_style_changed` se colore/contorno; altrimenti redraw completo; `model_settings_changed` se intervallo/ordine/shake. Risposta `STATUS{OK}` o `BAD_FORMAT` |
| `ALBUM_ORDER{ORDER 12 B}` | qualsiasi | indici < 12 senza duplicati, poi solo 0xFF → `storage_set_order` → `model_album_changed` (differito se in SYNCING) → `OK`; altrimenti `BAD_FORMAT`. **`order[]` è la lista autorevole della rotazione**: uno slot VALID non elencato non viene mostrato (S5b deve inviare sempre l'ordine completo; `storage_commit_slot` aggiunge in coda gli slot nuovi non presenti) |
| `ALBUM_DELETE{SLOT}` | qualsiasi | `SLOT < 12` → `storage_clear_slot` (state EMPTY, chunk lasciati, `order[]` invariato: la rotazione filtra per VALID) → `model_album_changed` → `OK`; se era lo slot del `pending`, il trasferimento decade |
| altro `MSG` | qualsiasi | `STATUS{NOT_SUPPORTED}` |

**Timeout di silenzio**: 30 s senza messaggi in SYNCING → IDLE, foto in corso abbandonata, "Foto k/n" spento (`sync_proto_timeout`, AppTimer riarmato a ogni messaggio con `app_timer_reschedule`; se il timer è già scaduto con il callback ancora in coda dietro il messaggio in corso, `reschedule` ritorna false → `app_timer_cancel` + nuovo `register`, e un contatore di generazione passato come contesto fa ignorare un eventuale callback stantio, che altrimenti forzava un IDLE spurio subito dopo un messaggio — revisione 29/08, F2; stesso schema nel debounce delle impostazioni di `storage.c`). Un `PHOTO_BEGIN` successivo riceve `BUSY`: il PKJS rinnova la `SYNC_REQUEST{COUNT, OFFSET = photoIndex}` e riprende dall'offset corrente (il `pending` è perso, quindi la foto riparte da 0, ma "Foto k/n" riprende da k).

**Riga info**: "Foto k/n" (`ui_time_set_sync_progress`) al posto di passi/icona BT nel layout A per tutta la sync (k = foto in corso, n = `COUNT`; dopo un BUSY riparte da `OFFSET` della `SYNC_REQUEST`; resta indietro di uno per ogni foto saltata dal telefono senza un `PHOTO_BEGIN` accettato — cosmetico, §10); in B nulla.

**Lato telefono (`src/pkjs/sync.js`, S5a; album vero in S5b)**: `ready` → `JS_READY`; **HELLO atteso entro 10 s dall'ACK** del `JS_READY` (l'HELLO nasce solo dal `JS_READY` e l'outbox dell'orologio non ritenta: 2 rinvii di `JS_READY`, poi `provider.onReadyFailed(why)` → retry lungo di `index.js`; nessun rinvio se l'HELLO arriva prima dell'ACK o se ha `PROTO` diverso — revisione 29/08, F5); `HELLO` → `provider.plan(hello)` = `{photos, settings, order, deletes}` (formato da `HELLO.FORMAT`; decodificare base64url **solo** le foto da inviare, una per volta); prima `SETTINGS`/`ALBUM_ORDER`/`ALBUM_DELETE` (ognuno attende il suo `STATUS`), poi `SYNC_REQUEST{COUNT}` → `SYNC_READY` → per ogni foto `PHOTO_BEGIN` → `STATUS{OK, OFFSET}` → `PHOTO_DATA` da `OFFSET` a passi di `MAX_CHUNK` (il successivo dal callback di successo, mai da timer) → `PHOTO_END{SLOT, PHOTO_ID}` → `STATUS` → foto successiva → `SYNC_DONE`. Regole che reggono il PKJS (S5a, verificate in emulatore): **un solo `PHOTO_DATA` in volo** (dopo una ripartenza il messaggio di ripresa può partire mentre l'ultimo chunk della generazione precedente è ancora in volo: il suo ACK viene ignorato), ma lo `STATUS` di un chunk fuori sequenza arriva *dopo* l'ACK di quel chunk (quindi dopo l'invio del successivo): il flusso dati ha una **generazione** `gen` che cambia a ogni ripartenza (`SEQ_ERR`/`BUSY`/`CRC_ERR`) e fa ignorare l'ACK dei chunk spediti prima; **ogni gestore di STATUS rifiuta le risposte non pertinenti** (S5b: prima di tutto per `REPLY_TO ≠ MSG atteso`; poi `SEQ_ERR`/`CRC_ERR` mentre si attende l'OK di un `PHOTO_BEGIN`; `OK` con `OFFSET ≠ LENGTH` mentre si attende quello dell'`END`; `SLOT ≠ 0xFF` per `SETTINGS`/`ALBUM_ORDER`, `SLOT ≠ slot` per `ALBUM_DELETE`; tutto tranne `NOT_SUPPORTED`/`BAD_FORMAT`/`STORAGE_ERR` mentre si attende `SYNC_READY`) lasciando intatta l'attesa e il suo timer; un `SYNC_READY` ripetuto non tocca l'attesa di un altro messaggio. Backoff 1-2-4-8-8-8-8 s sui fallimenti di `sendAppMessage`, poi (S5b) **la sync si chiude con errore** (telefono scollegato: inutile provare le foto rimaste una per una) e `index.js` la ritenta con backoff lungo 30 s-1-2-5-10 min (`JS_READY` → `HELLO` → diff: costa nulla se non c'è niente da fare), azzerato da una sync riuscita — **tre classi di esito** (29/08, F9): *link* (`summary.error` = `send failed`/`watchdog`, o `JS_READY` non consegnato) senza tetto = ripresa alla riconnessione; *photo* (esiti per foto/impostazioni/ordine/eliminazione con codice non permanente: `STORAGE_ERR`, `CRC_ERR`, `BUSY`, `load: …`; `summary.photoCodes = ['slot:CODE', …]`) al più 3 retry consecutivi (`photoRetries`, azzerato da un esito *link* o da una sync riuscita) poi si rinuncia fino al prossimo HELLO spontaneo; *permanent* (`NOT_SUPPORTED`, `BAD_FORMAT`, `NO_SPACE`) nessun retry; `STATUS` atteso entro 10 s (2 rinvii, poi `send failed`); `SEQ_ERR` → `PHOTO_BEGIN{OFFSET = status.OFFSET}` e ripresa; `CRC_ERR` → una ripetizione da 0; `BUSY` → nuova `SYNC_REQUEST{COUNT, OFFSET}` (l'orologio è tornato IDLE; se la richiesta di ripresa non viene consegnata la sync si chiude come per ogni invio fallito, non scarta la foto — 29/08); altri codici → foto scartata; a fine sync **nessuna attesa di STATUS sopravvive** (`cancelWait` in `finish`: uno STATUS tardivo non richiama un gestore stantio né riapre un'attesa — 29/08, F7); watchdog 60 s **senza ACK né messaggi dall'orologio** (il motore ha smesso di spedire: p.es. `load` che non torna) → sync chiusa con errore (un `HELLO` successivo o il retry può ripartire). Base64url decodificato in JS puro (`b64.js`: nel PKJS non esiste `atob`).

**Tempi attesi** ⚠️: 34,2 KB in 9 messaggi ≈ 9 round trip (ACK dopo 16 `persist_write_data` ≈ 80–110 ms su PT2) + trasferimento a 5–15 KB/s ≈ **4–9 s per foto** (contro 3–8 con chunk da 7.936: +1 round trip al secondo circa); 12 foto ≈ 50–110 s una tantum. Emulatore: vedi `PIANO.md` §4 (esito S5a).

**HELLO, v1.7 (S5b)**: `HELLO` porta anche **`CRC u16` = CRC-16/CCITT-FALSE dei primi 18 B di `GalSettings` correnti** (`settings_get()`, cioè tutto tranne il campo `crc16`): il telefono confronta con il CRC del proprio blob e manda `SETTINGS` **solo se diverso** — la sincronizzazione delle impostazioni è stateless come quella delle foto (dopo un `pebble wipe` il CRC dell'orologio torna quello dei default e il telefono rimanda le sue; uno `STATUS` perso non lascia flag sporchi). Outbox di allora: 101 + 9 = 110 B esatto (`SYNC_HELLO_VALUE_BYTES` allora 67 in `sync_proto.h`, pin nel test host; 29/08) — dalla v1.9 sono **119 B** e **69** con `OPEN_MS`. Un telefono con PKJS vecchio ignora il campo; un orologio vecchio (senza `CRC`) fa mandare `SETTINGS` a ogni sync (innocuo).

**HELLO, v1.9 (04/09/2026, perf; nota del 05/09)**: `HELLO` porta anche **`OPEN_MS u16` = millisecondi della prima chiamata persist**, cioè dell'apertura del file da parte del firmware (2 scansioni) **più la ricerca della chiave 0**, la cui posizione dipende dalla storia del file: dopo i chunk della prima foto su un file nuovo, **in coda dopo una migrazione 1 → 2** (fino a una scansione in più; F04). `storage_init` la prende **sempre**, anche nella build di produzione: **0 = apertura sotto il millisecondo**, campo assente = orologio pre-v1.9. Serve solo a **diagnosticare**: il firmware scandisce tutto il file a ogni apertura e non lo ripulisce finché non supera ~615 KB, quindi un file pieno di record morti porta l'avvio della watchface da ~0,3 s a 2–3 s e l'unico rimedio in mano all'utente è rimuovere e reinstallare Galleria dall'app Pebble (l'album resta nel telefono, le foto tornano da sole). Il telefono lo passa in `hello.openMs` (`null` se il campo manca = orologio pre-v1.9), lo salva nello snapshot `galleria.v1.watch` e la **config page** mostra l'avviso `#slow` con la procedura quando supera una soglia **proporzionale al numero di foto valide** dello snapshot (`slots[k].state == 1`): **400 + 100 × n ms** — 0 foto 400, 4 foto 800, 12 foto 1.600 —, perché la ricerca della chiave 0 cresce con i record del file (§6). Riferimenti misurati sul Pebble Time 2 dell'autore: **90 ms** con 4 foto su file nuovo (nessun avviso), **2.145 ms** con lo stesso album su file gonfio (avviso sì); 12 foto sane stimate 650–1.200 ms (nessun avviso). Nessuna decisione dell'orologio dipende da questo campo; un telefono con PKJS vecchio lo ignora.

### 5.1 Lato telefono — album e diff (S5b, `src/pkjs/`)

Moduli ES5 (WebView Android, JavaScriptCore iOS, pypkjs): `index.js` (glue: eventi `Pebble`, modalità dev), `album.js` (**puro**: album sopra un'interfaccia `storage` `{getItem, setItem, removeItem}`, testabile in node), `sync.js` (motore §5, S5a) + `b64.js`, `crc.js` (CRC-32 zlib e CRC-16/CCITT-FALSE, stessi vettori di `crc.h`), `devserver.js` (client XHR del dev server, solo con `Pebble.platform === 'pypkjs'`). Bundle webpack con entry `index.js`: i moduli non richiesti (p.es. `test/fixture_photo.js`) non entrano nel `.pbw`.

**Album in `localStorage`** (nel PKJS dell'app Pebble: Android `SharedPreferences`, iOS `NSUserDefaults`; in pypkjs **persistente** in `~/.local/share/pebble-sdk/4.33.1/<piattaforma>/localstorage/<uuid>.{dat,dir}` — la nota di S5a "in emulatore non c'è" era sbagliata. ⚠️ È un `dbm.dumb` di CPython: l'indice `.dir` viene aggiornato solo per le chiavi nuove e alla chiusura pulita del runtime JS (che avviene a ogni `pebble install`), **non** per la riscrittura più lunga di un valore esistente ⇒ dopo `pebble kill` (SIGKILL) il JSON riletto è troncato. In modalità dev `index.js` fa quindi `removeItem` + `setItem` a ogni scrittura (`__delitem__` → `_commit()` riscrive `.dir`; la chiave riscritta passa da `_addkey`): durabile per qualsiasi lunghezza e charset — il padding a 64 KiB usato fino al 29/08 contava code unit UTF-16 mentre `dbm.dumb` conta byte UTF-8, quindi `città` o un JSON a cavallo del multiplo lo vanificavano (revisione 29/08, F11); se `setItem` lancia, il valore vecchio viene rimesso. I valori già paddati si rileggono (JSON.parse e b64.decode ignorano gli spazi). Costo: `.dat` che cresce a ogni scrittura (`pebble wipe` azzera). `pebble wipe` cancella anche il `localStorage`: per "orologio azzerato, telefono intatto" cancellare solo `qemu_spi_flash.bin`):
```
galleria.v1.album        JSON {v: 1,
                               photos: [12 × (null | {id, name? (≤ 64 car.), fmts: {"1": {len, crc, stored}, "2": {len, crc, stored}}, thumb? (≤ 6.000 car.)})],   // indice = slot; stored (S7, F12) = payload su disco
                               order: [slot…],            // ordine di rotazione (lista autorevole: §5 ALBUM_ORDER)
                               orderGen: n,               // cresce a ogni cambio dell'ordine: un ALBUM_ORDER confermato azzera orderDirty solo se l'ordine è ancora quello
                               settings: {layout, font, clock_mode, leading_zero, text_color, outline, interval_min, order, shake_next, info_row},
                               settingsSet: bool,         // l'utente ha espresso le impostazioni almeno una volta (payload con settings)
                               deleted: [slot…],          // ALBUM_DELETE non ancora confermati dall'orologio
                               orderDirty: bool}          // ALBUM_ORDER da mandare alla prossima sync
galleria.v1.watch        JSON {at, format, maxChunk, settingsCrc, openMs, slots: [12 × {state, crc}], foreign: [slot…]}   // ultimo HELLO (stato per la config page); chiave separata: riscritta a ogni HELLO senza toccare l'album
                         // openMs (v1.9) = HELLO.OPEN_MS; null se l'orologio non lo manda o se lo snapshot è pre-v1.9 (nessun avviso nella pagina)
galleria.v1.p<slot>.<fmt>   payload base64url senza padding (fmt 1 = raw6, 2 = raw1); assente ⇒ "solo metadati" (dev: caricato a richiesta dal dev server)
```
Regole: `id` (= `photo_id`, uint32 ≠ 0) assegnato dalla pagina/dev server, altrimenti generato dal PKJS; `crc` = CRC-32 zlib del payload, salvato **senza segno** e inviato come `crc | 0`; JSON corrotto o `v` diverso ⇒ album azzerato (log), mai crash; **ordine delle scritture** (29/08, F6/F8): il payload nuovo si scrive PRIMA di toccare la foto vecchia dello slot (stessa chiave, si sovrascrive): `setItem` che lancia (quota) ⇒ quella voce non viene registrata e la vecchia resta intatta, metadati e payload; le rimozioni di payload (altro formato di una foto sostituita, voce dev senza `data`, slot eliminati) si eseguono solo DOPO il JSON dell'album; JSON che non si scrive ⇒ l'intera modifica è annullata (payload scritti rimossi o riportati al valore precedente, album riletto dal disco, `ok = false`, `changed = false`; `index.js` non fa `resync`); una foto a due voci (fmt 1 e 2) con quota su una sola resta registrata nel solo formato entrato ("da rifare il ritaglio", §10); cap 12 slot e 24 voci per payload, `data` accettata solo con la lunghezza base64 attesa (prima di decodificare), errori limitati a 32; una scrittura del JSON per `applyPayload`. Una foto del piano che risulta cambiata quando viene caricata (Save durante la sync) viene saltata (`load` → errore) e il prossimo `HELLO` rifà il piano; un payload locale corrotto viene rimosso insieme ai metadati del formato (senza loader) — mai una foto "zombie".

**Payload** (config page → `webviewclosed`, oppure dev server → `/state.json`), sempre `{v: 1, settings?, order?, deleted?: [slot…], photos?: [{slot, photo_id, fmt, len, crc, data?: <base64url>, url?: <solo dev>, name?, thumb?}]}`; una voce per **formato** (la stessa foto può avere due voci, fmt 1 e 2, stesso `photo_id`). `album.applyPayload(payload, {full})`:
- `full: false` (telefono, §6): delta — solo le voci presenti cambiano; `deleted` svuota gli slot elencati (metadati + payload) e li accoda a `deleted`; `order` sostituisce l'ordine; `settings` sostituisce le impostazioni.
- `full: true` (dev server; `index.js` lo usa solo se `state.full === true`): stato completo — gli slot **non** elencati vengono svuotati come se fossero in `deleted`, **ma solo se ogni voce è identificabile**: `photos` non array, voce non oggetto, slot non valido, troppe voci o payload interrotto ⇒ nessuna eliminazione implicita (errore `eliminazioni implicite saltate`); una voce rotta con slot valido conta come elencata e non tocca la foto locale (29/08, F10).
- Voce foto: `slot` 0..11, `fmt` 1|2, `len` = lunghezza del formato (34.200 / 3.024), `data` (se presente) decodifica a `len` byte con CRC-32 = `crc`, altrimenti scartata con log; **idempotente**: stesso `id` e stesso `crc` per quel `fmt` ⇒ nessuna modifica; `id` diverso ⇒ foto nuova nello slot (i payload dell'altro formato vengono rimossi); `order` assente ⇒ gli slot nuovi vengono accodati all'ordine; ogni cambiamento dell'album (foto nuova, eliminazione, riordino) mette `orderDirty`.
- `settings`: campi validati con gli stessi intervalli di `settings_validate()` (valori non validi ⇒ default del campo); nessun flag: il confronto è via `HELLO.CRC`.

**Piano** (`album.plan(hello)` = provider di `sync.js`, ricalcolato a ogni `HELLO`; `fmt = hello.format`):
1. Per ogni slot `k` con foto locale che ha `fmts[fmt]`: da inviare se `!(hello.slots[k].state === 1 && hello.slots[k].crc === fmts[fmt].crc)` (slot vuoto dopo un wipe, CRC diverso, foto sostituita) **e** il payload è disponibile — **S7 (F12): lo dicono i metadati** (`fmts[fmt].stored === true`, impostato da `setPayload` riuscito e rimosso col formato; `false` per le voci dev "solo metadati") oppure c'è un `loader` dev: `plan()`, `summary()`, `state()` e `applyPayload()` non leggono mai le chiavi dei payload (45,6 KB l'una, fino a 12 per HELLO; unica eccezione: i byte precedenti per il rollback F6/F8, riletti solo se i metadati dicono che esistono); un payload sparito alle spalle dell'album si scopre in `load()` → errore `payload assente`, `_dropCorrupt` rimuove i metadati di quel formato e il prossimo HELLO non la ripianifica (nessun ciclo di retry; con un loader i metadati restano e `stored` torna false); gli album scritti prima di S7 vengono migrati alla prima rilettura con una sonda `hasPayload` per formato, una sola volta, e riscritti (`v` resta 1); foto senza il formato dell'orologio (orologio cambiato) ⇒ log, nessun invio, nessun errore (la config page di S6 chiederà di rifare il ritaglio). Ogni foto del piano ha `load(cb)` invece di `bytes`: il motore decodifica **una foto per volta** e libera i byte dopo l'esito.
2. `deletes` = `deleted ∩ {k : hello.slots[k].state === 1}`; gli slot di `deleted` già vuoti sull'orologio escono subito dalla lista.
3. `order` (12 B, `0xFF` in coda) se `orderDirty` **oppure** il piano contiene foto o eliminazioni: ordine locale (solo slot con foto) + **slot VALID "estranei"** (sull'orologio ma non nell'album e non in `deleted`: p.es. telefono reinstallato) accodati, così restano visibili — **il telefono non elimina mai ciò che l'utente non ha eliminato**; gli slot estranei vengono annotati in `watch` per la config page.
4. `settings` (20 B, `crc16` compilato) se **`settingsSet`** e `hello.settingsCrc` è assente o diverso da `crc16(blob[0..18))` — un telefono che non le ha mai impostate (app reinstallata, `localStorage` perso) **non** sovrascrive con i default quelle dell'orologio; dopo il primo Save della config page il telefono è l'autorità.
5. `watch` ← snapshot dell'HELLO (v1.9: anche `openMs`, `null` se l'HELLO non lo porta). Esiti (`onDone(summary)`): `ALBUM_ORDER` OK ⇒ `orderDirty = false`; `ALBUM_DELETE k` OK ⇒ `k` esce da `deleted`; le foto non hanno flag (il prossimo `HELLO` conferma con il CRC). **S6**: gli esiti aggiornano anche lo snapshot (`onPhotoResult` OK ⇒ `watch.slots[k] = {1, crc}`; `ALBUM_DELETE k` OK ⇒ `{0, 0}` e `k` via da `foreign`), scritto con la sua chiave.
Prima sync dopo una modifica: `webviewclosed` ⇒ `applyPayload` ⇒ `sync.resync()` (= nuovo `JS_READY` → `HELLO` → piano; se una sync è in corso viene ripetuto al termine). `ready` ⇒ `sync.start()` (un secondo `ready` nello stesso runtime ⇒ `resync()`); in modalità dev prima `GET /state.json` (irraggiungibile ⇒ si parte con l'album locale: il percorso "riavvio senza dev server" è quello del telefono). Retry di `index.js` (30 s, 1, 2, 5, 10 min) per classi (§5, 29/08): *link* senza tetto, *photo* al più 3 consecutivi, *permanent* mai (nessun ciclo radio inutile); una sync riuscita cancella il timer.

**Modalità dev** (`Pebble.platform === 'pypkjs'`, `tools/galleria_devserver.py`, §6): il dev server fa le veci della config page: `/state.json` è un payload `full` con `url` al posto di `data`; `album.plan` usa un `loader(slot, fmt, cb)` che scarica `GET /photo/<k>.raw6|raw1?b64=1` e **salva il base64url in `localStorage`** (da lì in poi l'album è identico a quello del telefono); `state.hooks.scenario` (`seq`/`dup`/`crc`/`interrupt`) inietta i guasti di S5a nel motore (solo dev); `state.hooks.open_ms` (v1.9, `--open-ms N` del dev server) fa forzare a `index.js` `hello.openMs` prima del piano, per provare l'avviso `#slow`. `showConfiguration` ⇒ `Pebble.openURL('http://localhost:8765/config.html')` (il tool aggiunge `?return_to=http://localhost:<porta>/close?`); la pagina salva con `POST /save` e torna con `return_to + encodeURIComponent('{"v":1,"dev":true,"seq":N}')` (token: il ritorno passa dalla riga di richiesta di `http.server`, ≤ 64 KB); `webviewclosed` con `dev` ⇒ `GET /save.json` (= stato completo) ⇒ `applyPayload(full)` ⇒ `resync()`. Sul telefono (S6) `webviewclosed` porta il payload intero: `r.charAt(0) === '{' ? JSON.parse(r) : JSON.parse(decodeURIComponent(r))`, risposta vuota o non valida ⇒ ignorata con log.

## 6. Config page (S6)

Specifica operativa e contratti: **`galleria-s6-config-page.md`** (v1.0 del 29/08 + precisazioni del 30/08 nei §4, §6, §8, §9). Implementata il 30/08/2026 (S6):
- **File**: `src/pkjs/config/{page.html, page.css, pipeline.js, page_core.js, previews.js, page.js}` (ES5, typed array e canvas, nessuna dipendenza, **niente `localStorage`**: origine opaca) inlinati da `tools/build_config_page.py` in `src/pkjs/config_page.js` (`module.exports = "<html>"` ASCII; HTML **63.735 B** dopo lo strip delle sole righe di commento/vuote e dell'indentazione, modulo **65.761 B** (verificato con `build_config_page.py --check` il 05/09/2026, dopo l'avviso `#slow` con soglia proporzionale; era 58.141/59.987 a fine S6 e 62.893/64.899 dopo S8-stile); tetto **65.536 B sull'HTML**, obiettivo soft 60 KB **già superato** (margine residuo **2.112 B**: rimisurare a ogni aggiunta con `--check`); `make -C test pagecheck` = `--check` **prima di ogni `pebble build`**: se l'inlining fallisce il modulo vecchio resta sul disco). I sorgenti di `src/pkjs/config/` non entrano nel `.pbw` (webpack segue solo i `require`; verificato con un marcatore); `config_page.js` è richiesto pigramente da `index.js` in `showConfiguration`. Bundle PKJS 155,1 KB (era 90), `.pbw` 589 KB. `previews.js` = anteprime "12:34" dei 3 font (PNG 1 bit bianco su nero, 28 px, 1.722 B, `tools/gen_font_previews.py` v2; mostrate a dimensione naturale: `.fontprev` con `box-sizing: content-box`).
- **Stato PKJS → pagina nell'hash dell'URL** (`watch` compreso: v1.9 porta anche `watch.openMs`): `album.state()` + `{v:1, platform, fmt, cap_kb, dev}` → `b64.encodeUtf8(JSON)` (base64url dei byte UTF-8, ≈ 100 KB con 12 miniature); telefono `data:text/html;charset=utf-8,` + `encodeURIComponent(html)` + `#hash`; emulatore `http://localhost:8765/config.html#hash` (`emu-app-config` inserisce `?return_to=` prima dell'hash). `fmt` = formato dell'ultimo HELLO, altrimenti dalla piattaforma (`getActiveWatchInfo`); `cap_kb` 900 (Android/pypkjs) o 200 (iOS ❓).
- **Payload pagina → PKJS** (sempre lo stesso oggetto): `{v:1, settings, order, deleted, photos:[{slot, photo_id, fmt, len, crc, data, name, thumb?}]}` — `settings`/`order`/`deleted` sempre presenti, `photos` solo le nuove e **solo nel formato dell'orologio** (chiude il punto `[9]` di §10: mai due voci per foto); `applyPayload(payload, {full:false})` + `resync()`. Trasporto: telefono `pebblejs://close#` + `encodeURIComponent(JSON)`; emulatore `POST /save` → `{ok, seq}` → `return_to + encodeURIComponent('{"v":1,"dev":true,"seq":N}')` → il PKJS rilegge `GET /save.json` (payload senza `full`); pagina aperta a mano senza `return_to` = "modalità prova" (nessuna navigazione, `GalPage.lastPayload`). Annulla = risposta vuota. Tetto: `Math.ceil(JSON.length/1024) > cap_kb` ⇒ Salva disabilitato con il numero di foto da togliere.
- **UI** (una colonna, 360–400 px): tessere in ordine di rotazione (miniatura 50×57 JPEG ≤ 6.000 car. resa "come sul vetro", badge *da inviare* / *solo sull'orologio: resta in coda* (slot estranei da `watch.foreign`, sempre in coda con ▲▼ disabilitati: `applyPayload` ignora gli slot senza foto locale e `plan()` li accoda) / *manca il formato per questo orologio* / *nuova*, ▲ ▼ ✕ alti 40 px con `aria-label`), "Aggiungi foto" (`<input type=file accept=image/*>` senza `capture`, `createImageBitmap(file,{imageOrientation:'from-image'})` con fallback `<img>`), editor con **cornice fissa 200:228** e immagine che si sposta/ingrandisce sotto (Pointer Events + pinch + rotellina + slider + "Adatta", fallback mouse/touch; con `fmt 2` il sotto-rettangolo flint 144:168 tratteggiato), ridimensionamento a dimezzamenti su canvas con fondo bianco, "Luminosità (gamma)" 0,5–2 e "Schiarisci le ombre (lift)" 0–0,3, dithering FS/Bayer/nessuno (emery) o FS/Atkinson/nessuno (flint), "Ottimizza per il vetro" (LUT sunlight, OFF), **anteprima ×2** con toggle "come sul vetro"/"colori nominali" (debounce 150 ms), impostazioni complete (`s_<campo>`, LECO solo nel layout A con ritorno a Anton, anteprime font), avviso "impostazioni non ancora salvate" se `settingsSet` è falso, contatore "Da inviare: N KB / M KB", Salva/«Esci senza salvare» sticky (uscita in due tocchi con modifiche pendenti; «Annulla ritaglio» nell'editor). Con stato non ricevuto (hash assente/rotto) Salva resta disabilitato (revisione S6 #1: un payload di default azzererebbe ordine e impostazioni). Tetto iOS (200 KB) deciso dalla pagina via `GalPageCore.capForUa` (user agent), perché `Pebble.platform` è il runtime. `window.GalPage` (stato, tessere, `setNavigate`, `addFile`, `buildPayload`, `lastPayload`) per test e gate.
- **Pipeline** (`pipeline.js`, puro): porting funzione per funzione di `tools/photo_prep.py` — `test/test_pipeline.js` (439 asserzioni, fixture `test/fixture_page.js` generata da `test/gen_page_fixture.py` importando il tool: 27 combinazioni dithering × LUT × (gamma, lift) identiche byte a byte, `pack6`/`pack1` sulle fixture del test C, LUT 32³ CRC `0x48CBD990`, 16 mutanti su 16 uccisi). Tempi: FS 200×228 0,8 ms e LUT 12 ms in node; in Firefox ridimensionamento 3–15 ms (12 MP) e codifica ≈ 3 ms. Le soglie di `q(v)` sono 42|43, 127|128, 212|213 (`(v+42)//85`).
- **Avviso di avvio lento** (v1.9, 04/09; soglia rivista il 05/09): se lo snapshot dell'HELLO porta `openMs` sopra **400 + 100 × n ms** (n = foto valide in `watch.slots`) la pagina apre con il riquadro `#slow` (classe `warn`, in cima): «Galleria si avvia lentamente (X s). Non è un guasto: la memoria dell'orologio si è riempita di vecchi dati.» + la procedura in 4 passi (app Pebble → Galleria → **Rimuovi** → reinstalla) + «Le tue foto sono al sicuro nel telefono e torneranno da sole sull'orologio in pochi minuti (circa mezzo minuto per foto).» (F06, 05/09: con 12 foto il trasferimento dura ~5 min, non «circa un minuto» come diceva la v1.9); la stessa procedura, con la spiegazione del perché, sta in fondo alla pagina nella sezione `#help` («Galleria si avvia lentamente?», sempre presente, ripiegata, aperta da un pulsante). Soglia in `GalPageCore.slowThresholdMs(watch)` = `SLOW_BASE_MS` 400 + `SLOW_PER_PHOTO_MS` 100 × n (n = slot con `state === 1`); `openMs` null/0 o non oltre la soglia ⇒ nessun avviso. In emulatore l'apertura è istantanea: `galleria_devserver.py --open-ms N` (hook `hooks.open_ms` → `index.js` in DEV forza `hello.openMs`) serve a provarlo.
- **Snapshot dell'HELLO** (`galleria.v1.watch`): da S6 viene aggiornato anche dagli esiti della sync (`album.onPhotoResult` OK ⇒ `slots[k]` VALID con il CRC inviato; `ALBUM_DELETE k` OK ⇒ slot vuoto e via da `foreign`), così la pagina aperta subito dopo non mostra "da inviare" ciò che è appena arrivato.
- **Emulatore**: `tools/galleria_devserver.py --page-dir src/pkjs/config` (inlining a ogni GET, **modalità relay** automatica: `/state.json` senza `full`, `POST /save` della pagina con validazione severa, `/save.json` senza `full`; `--relay` esplicito; il server "nudo" resta `full` con 0 foto — avviso all'avvio — per `test_devpage.js`). Gate nel browser con `tools/galleria_browser.py` (WebDriver stdlib → Firefox snap headless via `geckodriver`: file per `<input type=file>` sotto `$HOME` e **fuori dai dot-dir**, finestra ≥ 500 px → `narrow URL 400`, URL della pagina letto dal file temporaneo di `emu-app-config`).
- **Test**: `test_page.js` 898 asserzioni sui sorgenti + 919 sull'HTML inlinato di `config_page.js` (page_core + page.js in `vm` con DOM/canvas/XHR finti; struttura del modulo generato), `test_pipeline.js` 439, `smoke_s5b.js` step 10 (stato nell'hash), `test_album.js` (snapshot), `build_config_page.py --selftest` 83, dev server 230, browser 60, font previews 29; `make -C test` ≈ 35 s.

Pipeline JS (snippet completi in `docs/ricerca/galleria/05-colore-quantizzazione.md` §1–2): downscale a dimezzamenti + passo finale → tono → FS serpentine fixed-point (spazio raw o LUT 32³ "sunlight") → `pack6`/`pack1` → CRC32 → base64url. Round-trip Python (`photo_prep.py`) ↔ JS: stessa immagine ⇒ stesso raw6 (test S6). **S2:** il riferimento è `tools/photo_prep.py` v1 (`tools/README.md` §9): fixed point ×16, serpentine, `>>4` (floor), clamp 0..4080, `q(v)=min(3,(v+42)//85)`, LUT 32³ costruita in **virgola mobile** (`R = r*255/31`, distanza euclidea; in interi 2 celle su 32.768 cambierebbero; CRC32 della LUT `0x48CBD990` nel selftest), arrotondamenti con `Math.round` (`.5` → +∞, **non** `round()` di Python); flint: sotto-rettangolo centrato 144:168 del crop, grigio `(54R+183G+19B)>>8`, soglia 2048; il ridimensionamento (LANCZOS nel tool) resta fuori dal round trip: il test si fa sull'immagine 200×228 già ridimensionata.
Piano B (app senza file chooser: Pebble < 1.8.0.7, Gadgetbridge; iOS se il pannello non funziona): campo "URL immagine" (proxy `wsrv.nl` come Fields of Gold) — v1.1.

## 7. Rendering (S1–S3)

- Un solo `Layer` con `update_proc`: `full_redraw ? blit foto intera : blit sub-bitmap della fascia` → sprite (`GCompOpSet`, palette `[1]` riempimento `[2]` anello `[3]` ombra 3D) → `GCompOpAssign` → riga info (`graphics_draw_text`). Antialias off. Un `layer_mark_dirty()` per tick. Buffer testo `static`. `GSize` del testo memorizzata (⚠️ `text_layer_set_should_cache_layout` non esiste nell'SDK). **S2 (`ui_photo.c`):** un solo `GBitmap` creato in `init()` per primo (`gbitmap_create_blank` 8Bit su emery, stride 200; `gbitmap_create_blank_with_palette` 1BitPalette con palette `static {nero, bianco}` su flint, stride 18), riempito con una sola lettura nella coda del bitmap + decodifica in place (`PhotoDecoder`); `ui_photo_set_band(0, band_h)` in `window_load` crea la sub-bitmap (`gbitmap_create_as_sub_bitmap`, nessuna copia) che `ui_photo_draw_band` blitta al tick con `graphics_draw_bitmap_in_rect(ctx, sub, band_rect)`; `ui_photo_draw_full` nei redraw completi; senza bitmap o senza foto → riempimento nero della fascia/schermo (mai cifre sopra cifre). `ui_time_photo_changed()` = luma a freddo (reset) + stile + redraw; `ui_time_band_changed()` (S3, isteresi) e `ui_time_style_changed()` (S6). Verificato in emulatore: il diff prima/dopo il tick tocca solo le righe delle cifre. Build di test: `GALLERIA_DEFINES="GALLERIA_DEMO_INDEX=1 GALLERIA_DEBUG_OUTLINE=1" pebble build` (wscript).
- Strip sprite: risorsa `bitmap` `2BitPalette` + `spaceOptimization: "memory"`, file con tag `~color`/`~bw`; **S3 misurato (pbpack)**: emery A 7.496 B, B 16.912 B (11 celle anche in B: la cella del `:` è inutilizzata), flint A 3.404 B, B 8.464 B. Layout A: solo la strip A in heap; layout B: solo la B, con la A caricata per la durata della Quick View (S7, D16; prima A + B fisse); caricate in `window_load`, al cambio font/layout (`ui_time_layout_changed`) e al `did_change` dell'area non ostruita, mai in `update_proc`; 11 sub-bitmap per la taglia A, 10 per la B (S7: strip B senza la cella del `:`, `ink[10] = {0,0}` → `ui_digits` salta i glifi con `w == 0`; pbpack B emery 15.376 B, flint 7.696 B). Palette del `.pbi` in ordine arbitrario → `ui_digits` riconosce gli indici **dal colore** (0xFF riempimento, 0xC0 anello, 0xF0 ombra; colore assente = indice assente, strip senza ombra) e li riscrive a runtime con `ui_digits_set_palette(size, fill, ring, shadow)` (ognuno può essere `GColorClear`). **S8-stile (D20/D21/D23):** quattro indici — anello spesso **R** (2 emery / 1 flint) e ombra 3D profonda **S** (2 emery, 0 flint) — e i quattro stili (pieno, trasparente, trasparente 3D, pieno 3D) sono **solo palette**, applicati da `ui_time.c:prv_apply_text_style()`: nessuna risorsa né blit in più. Su flint la strip ha **3 colori** (D26): nel `.pbi` di una piattaforma B/N la SDK passa ogni pixel per `nearest_color_to_pebble2_palette` (luma → 0/255, alpha → 0/255), restano 0x00/0xC0/0xFF e il rosso dell'ombra si fonderebbe con l'anello; gli stili 3D valgono come i corrispondenti piatti. `ui_digits_draw` centra nel passo il **nucleo** (`ink.w − shadow`): l'ombra sporge a destra e il riempimento resta centrato in tutti gli stili. **S8-stile misurato (pbpack, 10 strip):** emery A 7.288 / 6.784 / 7.144 / 7.504 / 7.360 B e B 13.316 / 12.216 / 13.816 / 14.916 / 13.816 B per Anton / Bebas / Barlow / Francois One / Staatliches (**108.268 B** in tutto); flint A 2.744 / 2.392 / 2.788 / 2.876 / 2.744 B e B 5.264 / 4.816 / 5.520 / 5.776 / 5.520 B (**44.548 B**).
- `gen_digits.py` **v2** (interprete `~/.local/share/uv/tools/pebble-tool/bin/python`: freetype-py + Pillow; `tools/README.md` §10; comando canonico `--fit-width --no-colon-b --pack`): tabella `FONTS` data-driven (5 voci) e `GEOM[(piattaforma, taglia)] = (cell_w, righe del riempimento, R, S)` — emery A (40, 66, 2, 2) e B (64, 94, 2, 2), flint A (28, 42, 1, 0) e B (48, 62, 1, 0), quindi **`strip_h = righe + 2R + S`** (emery 72/100, flint 44/64) con il riempimento nelle righe `R .. R + digit_h − 1`, baseline comune. Px cercata per ogni (font, taglia, piattaforma), anello = R dilatazioni 8-connesse meno il riempimento, ombra = `∪_{k=1..S} shift(riempimento ∪ anello, +k, +k)` meno `riempimento ∪ anello`, PNG RGBA a **4 colori esatti** (3 dove S = 0); `src/c/digit_metrics.h` generato (`strip_w/h`, `cell_w` = passo del layout, `digit_h`, **`ring`**, **`shadow`**, `px`, `ink[11]` = x/w dell'inchiostro *riempimento ∪ anello ∪ ombra*). `--fit-width` riduce la px quando il **riempimento** non entra nella cella (D24; Barlow `4`: 42 px a 66 di altezza → taglia A a 61 px su emery, 40 su flint). Controlli con la stessa formula di `ui_time.c` (**D25 terza versione**, `grid_steps`/`place_row_fit`: griglia uniforme = `max(cella, nucleo della cifra più larga + gap)` per le cifre e `max(cella del ':', nucleo del ':' + gap)` per il `:`, con `RING_GAPS` = 2, 1, 0 accettati entro `schermo − 2·FIT_MARGIN` (`FIT_MARGIN = 2`) e in riserva `max(cella, riempimento più largo + R)`; l'etichetta della riga negli avvisi e nell'header porta `[gap N]` o `[gap riserva]`): 24 h «20:44», 12 h «10:44» e 12 h «09:44» (zero iniziale) + 4 + PM (18 px su entrambe le piattaforme) entro la larghezza dello schermo (errore se no, e solo sulla riserva), sporgenza di anello/ombra del blocco centrato solo AVVISO, anello o ombra tagliati dalla cella di lavoro o PNG con più di 4 colori = errore.
- Colore automatico: D7, `luma.c`; ricalcolo a ogni cambio foto/fascia/layout, mai nel tick.
- flint: foto 1BitPalette `GCompOpAssign`; sprite palettizzati con `GCompOpSet` (mai bitmap 1Bit non palettizzati per gli sprite: lì `GCompOpSet` è un OR).

## 8. Budget

| | emery (PT2) | flint (P2 Duo) |
|---|---|---|
| Statico | obiettivo ≤ 30 KB (tetto 40; Fields of Gold 8,5 KB, TimeStyle 25 KB) — S2: 8.918 B; S3: 12.875 B; S6: 25.476 B; S7: 25.004 B (.text 23.116, .data 108, .bss 1.528); **S8-stile: 27.020 B** (.text 24.876, .data 112, .bss 1.776: 2 font in più nelle tabelle generate + stile delle cifre) | ≤ 30 KB (tetto 45) — S3: 12.755 B; S7: 24.892 B; **S8-stile: 26.908 B** |
| Heap disponibile (`131.072 − statico − 24` / `65.536 − statico − 28`) | ≈ 101 KB | ≈ 35,5 KB |
| Foto residente (preallocata in `init()`, per prima) | 45.640 B | 3.072 B |
| Strip sprite attiva (pixel della strip = `strip_w/4 × strip_h`; heap = questi + le sub-bitmap; pbpack in §7) | S3: A 8.152 B; layout B = A + B 25.500 B; S7 (strip compatte, D16): A ≈ 6.572 B (360×68), B Anton ≈ 12,3 KB (492×96), la A solo sotto Quick View; **S8-stile (anello 2 px + ombra 2 px)**: A Anton **404×72 = 7.272 B** (Francois One 416×72 = 7.488), B Anton **532×100 = 13.300 B**, B Staatliches **552×100 = 13.800 B**, B Francois One **596×100 = 14.900 B** (caso peggiore); LECO 0 | S3: A 4.060 B; B 12.960 B; S7: A ≈ 3.180 B (248×44), B ≈ 5,5 KB (328×64); **S8-stile invariato** (R = 1, S = 0, `strip_h` 44/64): A Anton 248×44 = **2.728 B**, B Anton 328×64 = **5.248 B**; A Francois One 260×44 = 2.860 B, B Francois One 360×64 = 5.760 B |
| Finestra, layer, sub-bitmap, font info | ≈ 1 KB | ≈ 1 KB |
| AppMessage (S5a: inbox unica 4.153 + outbox 128, misurato) | **4.384 B** | **3.360 B** (inbox 3.129) |
| **Totale a regime → margine** | S3 misurato: layout A liberi 62.204 B, B 46.908 B; S5a: A 45.892 B, B 30.388 B; S7 misurato: A liberi 47.072 B, B 43.472 B; **S8-stile misurato: A liberi 43.892 B (Anton) / 43.676 (Francois One), B 39.952 (Anton) / 39.452 (Staatliches) / 38.352 (Francois One)** — ⚠️ **deviazione aperta**: in layout B il vincolo ≥ 40 KB di S7 non è più rispettato (−48 B con Anton, −1.648 con Francois One); costo di statico (+2,0 KB) e strip più alte/larghe (anello 2 px + ombra 2 px). Rimedi possibili se servisse: S = 1 nella taglia B, oppure niente ombra in B | S3: A 43.872 B, B 36.608 B; S5a: A 28.160 B; S7: A 28.640 B, B 28.208 B; **S8-stile: A 26.612 B, B 25.668 B (Francois One)** |
| In sync | nessun costo aggiuntivo (inbox sempre aperta; heap identico a ogni messaggio e a fine sync, misurato S5a) | idem |
| Stack | 4 KiB: nessun array > 200 B; nessun buffer di staging (decodifica in place) | 2 KiB |
| Risorse | S3 misurato: 6 strip 73.224 B + 2 demo 68.400 B + resto = 145.732 B; S7: 125.200 B (6 strip compatte); **S8-stile: 176.668 B** = 10 strip 108.268 B + 2 demo 68.400 B (tetto 256 KB, 69 %) | S3: 45.760 B; S7: 33.680 B; **S8-stile: 50.596 B** = 10 strip 44.548 B + 2 demo 6.048 B |
| Persist | ≈ **430 KB** (41 %; allocazione 512 KiB) | ≈ 38 KB |
| CPU per tick | **S7 misurato in emulatore** (`GALLERIA_DEBUG_TIMING`, non rappresentativo dell'hardware): layout A Anton full 3 ms / fascia 1–2 / `tick` 4 poi 1; A LECO 2 / 1 / 3 poi 1; layout B 3 / 1–2 / 0 | **S7 emulatore: A Anton full 12 / fascia 11–12 ms; A LECO 22 / 20–22; B 2 / 0** — la riga info con font di sistema + alone 4× per voce costa più delle cifre sprite: da misurare sul Pebble 2 Duo in S8 (rimedi: riga info in cache, alone ridotto) |
| Cambio foto ⚠️ | 134 letture ≈ 10 ms (file piccolo) … 100–200 ms (file 512 KiB) + unpack 2 ms + luma < 1 ms; S7 emulatore: `photo: slot … persist crc ok 134 ch 5–9 ms`; sync in B: 9 messaggi/foto, chunk max 49–103 ms avg 21–54 (emulatore) | trascurabile (12 chunk, 1 ms) |

Mai `realloc`; mai `malloc` in `update_proc`; blocchi grandi una volta sola (in emulatore una `malloc` da 45,6 KB è fallita per frammentazione con 57 KB liberi).

## 9. Verifica (rimandi)

Gate per sessione in `apps/galleria/PIANO.md` §4; matrice `PIANO-SVILUPPO-PEBBLE.md` §12.1. Test host (`make -C apps/galleria/test`): `timefmt` (58), `crc` (614), `photo_codec` (263, con fixture `test/fixtures/rt.*` generate da `photo_prep.py --fixture` e verificate via CRC32: round trip Python→C), `luma` (99) + `photo_prep.py --selftest` (49); `rotation` da S4; `storage`, `sync_proto`, `model` e (S7) **`sync`** (`sync.c` inalterato con lo shim AppMessage/dict: 20 scenari, 1.400 asserzioni) con lo shim `test/shim/`. Test node del PKJS (S5b, `make -C apps/galleria/test jstest`, `NODE_PATH=shim`): `test_b64.js` (2.444), `test_album.js` (S7: 1.112, storage finto con guasti e contatore di `getItem`), `test_sync_engine.js` (689, orologio finto `shim/fakewatch.js` + `Pebble` finto), `smoke_s5b.js` (69), `test_page.js` (S7: 1.027 + 1.048 con motore minimo di cascata CSS); `tools/galleria_devserver.py --selftest` (230). Emulatore (S5b): dev server come config page (`--scenario` per i guasti), ritorno della pagina simulato con `curl` sulla porta di `emu-app-config` (`BROWSER=true`), flash dell'orologio cancellata da sola per "orologio azzerato, telefono intatto"; `emu-bt-connection --connected no` **non** usabile a metà sync (emulatore irraggiungibile dal tool); `emu-tap`, `unobstructed`. Telefono/orologio (S8, **Android per primo** — decisione del 30/08/2026, poi S8b iOS): specifica `docs/design/galleria-s8-hardware.md` (obiettivi O1–O11: collegamento `--phone IP`/`--adb`/`--cloudpebble`, sync 1→4→8→12 foto con file persist ≈ 140/280/430 KB, lettura da persist con file grande, timing reale `draw … info`/`tick` con la build M, colore automatico su 20 foto + test card `tools/gen_test_cards.py`, LUT sunlight vs foto del vetro, batteria 48 h, BT off/on, D5, config page sul telefono, flint), runbook `galleria-s8-runbook-android.md`, risultati in `galleria-s8-risultati.md`; log riassunti da `tools/galleria_logstats.py`.

## 10. Rischi e punti aperti ❓

- (29/08, revisione post code-review, da riguardare a fine progetto) "Foto k/n" resta indietro di uno per ogni foto saltata dal telefono senza `PHOTO_BEGIN` accettato (load fallito, `BAD_FORMAT`/`NO_SPACE` sul BEGIN): cosmetico; rimedio = portare k nel `PHOTO_BEGIN` (chiave `COUNT` riusata) o rinnovare la `SYNC_REQUEST{OFFSET}` dal PKJS. ~~Payload a due voci (fmt 1 + 2) con quota su un solo formato~~ → chiuso in S6: la config page manda **un solo formato per foto** (quello dell'orologio collegato), `applyPayload` resta com'è. ~~`plan()` legge ogni payload solo per `hasPayload` (F12)~~ → chiuso in S7 (`stored`). ~~`sync.c` non è compilato su host~~ → chiuso in S7: `test/test_sync.c` (19 scenari, 1.266 asserzioni, shim AppMessage/dict byte-esatto in `test/shim/`) pinna F1, F2, F4, coda outbox, decodifica per lunghezza di tupla e negoziazione; limiti dello shim: un solo AppTimer per processo (debounce impostazioni e timer di silenzio non coesistono nei test), callback sincroni, nessun costo di heap di `app_message_open`. Fatto verificato su PebbleOS `app_message_outbox.c` (30/08): `app_message_outbox_send` ritorna errore sincrono solo per `BUSY`/`INVALID_STATE`/`BUFFER_OVERFLOW` (solo quest'ultimo lascia `OUT_WRITING`: irraggiungibile per noi, il tripwire `dict_write` scatta prima), la disconnessione arriva sempre asincrona come `NOT_CONNECTED` in `outbox_failed` (gestita: coda svuotata).

1. **iOS** mai validato: pannello file nella WebView dell'app Pebble (S8 ricerca: l'app iOS non gestisce esplicitamente `<input type=file>`, issue #297 aperta; URL `data:` da ~100 KB in WKWebView mai provato: `URL_DATA_PREFIX` dichiarato ma non usato), lunghezza massima del close URL (nessun limite nel codice dell'app), PKJS in JavaScriptCore senza garanzie in background. S8b dopo Android. Piano B = campo URL (v1.1).
2. **Throughput BLE** e **costi persist con file grande** stimati; se la scansione per chiave nuova fosse ~1 s → 8 slot; compattazione bloccante durante una sync (coperta dal backoff 45 s, da misurare).
3. ~~Riapertura inbox~~ → impossibile (S5a): inbox unica da 4.153 B. **S5b**: `localStorage` del telefono con 12 foto (≈ 550 KB di base64, un solo formato da S6) e retry con `setTimeout` lunghi in background: da verificare su Android/iOS in S8. **S8 ricerca (30/08)**: nell'app Core Devices il `localStorage` del PKJS è `SharedPreferences` (`coredevices.coreapp_pkjs_<uuid>`) su Android e `NSUserDefaults` (`PKJS-<uuid>`) su iOS, **riscritto per intero a ogni `setItem`** → costo O(album) per scrittura, misurato da `[config] payload applicato in N ms`; i log JS verso il tool passano da un canale da 2 con DROP_OLDEST (raffiche perse).
4. **LUT sunlight** (pannello 2015) → opzione OFF finché non confermata; soglie del colore automatico (77/25/46, 15 %, isteresi 10) da tarare su 10–20 foto. **S2/S4:** a ogni cambio foto (rotazione, shake, focus) `model.c` chiama `ui_time_photo_changed()` → `luma_reset` → decisione a freddo; l'isteresi resta solo per la stessa foto su fascia diversa (chiuso in S4).
5. `createImageBitmap` con orientamento EXIF nelle WebView (fallback: lettura tag EXIF 0x0112).
6. v2: PNG8 (più foto), 4BitPalette "modalità compatta", riga info in B, posizione ora in basso, campo URL immagine.

## 11. Riferimenti
- Ricerca: `docs/ricerca/galleria/01…07` (fonti primarie citate file:riga; `07` = brief con protocollo/struct/wireframe completi).
- Codice di riferimento: `github.com/ygalanter/fields-of-gold-pebble` (MIT: file input in Clay, PBI 4-bit, chunk + persist a slot, misure persist su PT2), `github.com/BrianEnders/pebble-photo-watchfaces` (pipeline canvas in pagina), `github.com/dezign999/img999` (URL + jpg.js in PKJS), `github.com/pebble-examples/pebble-faces` (pattern chunk ufficiale), `github.com/freakified/TimeStylePebble`, `coredevices/mobileapp` (`WatchappSettingsScreen*.kt`, `PKJSApp.kt`, `PebbleDeepLinkHandler.kt`).
- Font: `raw.githubusercontent.com/google/fonts/main/ofl/anton/Anton-Regular.ttf`, `.../ofl/bebasneue/BebasNeue-Regular.ttf`, `.../ofl/barlowcondensed/BarlowCondensed-Bold.ttf`, `.../ofl/francoisone/FrancoisOne-Regular.ttf`, `.../ofl/staatliches/Staatliches-Regular.ttf` (SIL OFL 1.1; provenienza pinnata in `apps/galleria/resources/fonts/README.md`).
